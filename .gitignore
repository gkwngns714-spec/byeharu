# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Playwright (browser test artifacts)
/test-results/
/playwright-report/
/playwright/.cache/

# Env files (keep .env.example committed, never real keys)
.env
.env.*
!.env.example

# Local-only secrets for migration apply (NEVER commit)
supabase/.secrets.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# agent worktrees (isolation:worktree) — never part of the repo
.claude/worktrees/
scratchpad/

# 0352 rehearsal inputs — a READ-ONLY capture of production catalog + function bodies.
# Never committed: the live engine is the authority, and a checked-in copy is a second one that
# drifts the first time a migration re-emits any of those bodies. Regenerate with
# `node scripts/capture-0352-rehearsal-inputs.mjs`.
scripts/.0352-inputs/

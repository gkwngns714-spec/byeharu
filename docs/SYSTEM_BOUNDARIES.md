# Byeharu — System Boundaries & Ownership Contract

> Companion to `docs/ARCHITECTURE.md`. This file is the **law of separation**.
> Approved 2026-06-16. Read before adding any table, function, or migration.

## Core law

Each system owns only its own responsibility and communicates through clear
server-side functions — **never** by directly changing another system's tables.

- **No system secretly controls another.** No spaghetti logic.
- **One sole writer per table.** Everyone else goes through that system's functions.
- **The cross-system call graph is acyclic.**
- **No direct client writes** to game state — only `SECURITY DEFINER` RPCs.
- **No combat logic inside movement.** No movement logic inside combat except
  *requesting* return via `Movement.create`.
- **No reward duplication.** No hidden state changes.

---

## 1. Master table → sole-writer matrix

| Table | Sole writer (owner) | Read access |
|---|---|---|
| `profiles` | **Auth** | owner |
| `sectors`, `zones`, `locations` | **Map** | public |
| `unit_types`, `game_config`, `item_types`, `support_craft_types`, `main_ship_hull_types` | **Reference/Config** (admin/migration) | public read-only |
| `zone_state`, `location_state` | **World State** (sole writer = the `worldstate_*` functions; `location_state.price_multiplier` (WORLD-BALANCE-P19, 0136) is written ONLY by `worldstate_tick`, DARK/no-op while `world_balance_enabled='false'` — the tick leaves it at the backfilled 1.0) | public |
| `bases`, `base_units`, `base_resources` | **Base** | owner |
| `fleets`, `fleet_units` | **Fleet** (sole writer — EXCEPT the ONE sanctioned Main-Ship port-entry commission shim, approved 2026-07-04: `port_entry_commission_build` (0080; called by `port_entry_commission_writer` and the dark `commission_additional_main_ship`, 0080/0091) inserts the commissioned ship's present/location fleet row, and `normalize_main_ship_dock` (0084) normalizes dock state on `fleets` — writes confined to the calling player's OWN rows. **Retirement condition:** this exception retires when the port-entry path is next reworked for a FUNCTIONAL reason; at that point the `fleets` writes MUST be repointed through a Fleet-exposed commission/dock function via a forward-only migration (re-deriving the PORT-ENTRY prosrc-md5 pins at that deploy gate) and this exception note deleted. It is NOT repointed today because the path's bodies are guarded by the FROZEN md5-pinned PORT-ENTRY production verifiers (`normalize_main_ship_dock` and `port_entry_commission_writer` — the build core's caller — are two of the three pinned bodies; see `docs/TRADE_FLEET_0C_VERIFIER_REPOINT.md`) and it is the ACTIVE first-ship onboarding writer — a behavior-identical CREATE OR REPLACE purely for boundary hygiene would invalidate deploy-gate pins and add live-path risk for zero functional gain) | owner |
| `fleet_movements` | **Movement** | owner |
| `location_presence` | **Presence** | owner |
| `combat_encounters`, `combat_rounds` | **Combat** | owner |
| `reward_grants` | **Reward** | owner |
| `combat_reports` | **Report** | owner |
| `build_orders` | **Production** (Training + SHIPYARD-1 hull orders, 0188 — ONE M4.5 serial queue, never a second timer system. Two row kinds under the `build_orders_kind_coherent` CHECK: unit orders (unit + base set, hull NULL — the 0036/0038 training writers, unchanged) and hull orders (hull set — FK `hull_build_recipes`, so ONLY a recipe-carrying hull is orderable and T0 `starter_frigate` can never be enqueued; unit + base NULL; quantity 1, `credits_spent` recorded, paid up-front), written ONLY by `production_start_hull_build` via the public `start_hull_build` wrapper, DARK behind `shipyard_enabled=false`. THE SEAM (the SHIPYARD charter): hull orders land 'waiting' and are INVISIBLE to the 0038 engine by construction — `production_start_next` picks through its `unit_types` join (a NULL-unit row never joins) and `process_build_queue` loops 'active' rows only, both prosrc-pinned by the 0188 self-assert — so a hull order is never promoted/delivered until SHIPYARD-2 re-creates the engine with the hull arm + the commission-core delivery. SHIPYARD-2 MUST also ship hull-aware `cancel_build_order` semantics (today's 0038 cancel would flip a waiting hull order to 'cancelled' refunding only `metal_spent` = 0 — the credits/items would be eaten; unreachable while dark, no hull order can exist before ACT-SHIPYARD) BEFORE the flip) | owner |
| `player_inventory`, `inventory_ledger` | **Inventory** | owner |
| `main_ship_instances` | **Main Ship** | owner |
| `trade_goods` | **Reference/Config** (admin/migration; Trade Market catalog) | public read-only |
| `market_offers` | **Reference/Config** (admin/migration; Trade Market price catalog — migration-seeded only (0085, idempotent seed), NO runtime writer) | public read-only (RLS public-read policy, no client write path) |
| `ship_cargo_lots` | **Trade Cargo** | owner |
| `player_wallet` | **Wallet** | owner |
| `trade_receipts` | **Trade Market** | owner |
| `trade_relief_claims` | **Trade Market** | owner (owner-read) |
| `port_item_demand` | **Reference/Config** (admin/migration; the SALVAGE port item buy-list — MIGRATION-SEEDED ONLY (0174, idempotent converge-on-reapply), NO runtime writer, ever — the `market_offers` 0085/0173 posture. BUY-side only (`unit_price > 0` = credits the port PAYS): no path sells items TO players, so no pump loop can exist by construction. Seeded ONLY over items that actually drop from combat (the 0041/0171 `pirate_loot_for_wave` table: scrap, pirate_alloy, weapon_parts, engine_parts, repair_parts); progression items (`captain_memory_shard`/`blueprint_fragment`/`artifact_core`) are NEVER sellable — excluded by omission, pinned by the 0174 self-assert + `salvage-market-proof`) | public read-only (RLS public-read policy, no client write path) |
| `salvage_receipts` | **Salvage Market** (sole writer = `sell_item_at_port` (0174) — the per-ship idempotent item-sale record, the `trade_receipts` 0086 shape point-for-point: unique (main_ship_id, request_id), keyed by ship NEVER player_id, replay returns the original envelope verbatim; a SEPARATE table because `trade_receipts.good_id` FKs `trade_goods` and is structurally trade-goods-specific — item sales cannot satisfy that FK. DARK behind `salvage_market_enabled=false` (0174) — the writer rejects before any read while false, so no row can exist today) | owner (owner-read via the owning ship; authenticated, not anon) |
| `haul_contract_templates` | **Reference/Config** (admin/migration; the HAUL contract-template catalog — MIGRATION-SEEDED ONLY (0176, idempotent converge-on-reapply), NO runtime writer, ever — the `market_offers` 0085/0173 / `port_item_demand` 0174 posture. 10 v1 templates over the 0066 ports × 0073 goods carrying the reward-formula params (`reward_base` + `reward_premium_per_unit` over the LIVE dest `market_offers.buy_price`), the qty range, duration, and generation `weight`; origin/dest are fixed port uuids or NULL = 'any' (resolved at generation time). Read DOWNWARD only by `haul_generate_offers` (0176)) | public read-only (RLS public-read policy, no client write path) |
| `haul_contracts` | **Haul Contracts** (writers now COMPLETE and closed — three functions in ONE owner system: (1) `haul_generate_offers()` (0176, re-created 0179 under parity discipline) — the SECURITY DEFINER **service-role-only** cron generator (jobname `haul-generate-offers`, hourly): it MINTS 'offered' rows (deterministic per (day, port, slot) — the pure-hash 0041 technique, `hashtextextended` over salted (day, port, slot); idempotent on the natural key unique (origin_location_id, offer_day, slot) + `on conflict do nothing`, so re-runs/racing fires create nothing), flips stale 'offered'→'expired' (predicate status='offered' ONLY — the offer-expiry pass NEVER touches an accepted contract), and (the 0179 marked (a2) hunk) flips 'accepted' rows past `deliver_by`→'cancelled' — the SINGLE writer of the deadline-cancel transition (no penalty v1 [D]; frees the player's `haul_max_active_per_player` cap slot); within-deadline accepted rows are untouchable by EVERY pass (pinned by `haul-proof`); (2) `haul_accept_contract` (0179) — offered→accepted (a CLAIM: sets accepted_by/accepted_ship/accepted_at + `deliver_by = accepted_at + template duration_seconds`; moves NO cargo, NO credits); (3) `haul_deliver_contract` (0179) — accepted→delivered (docked-verified at the DEST + cargo debit via Trade-Cargo's `trade_cargo_consume` + `wallet_credit`, receipted). No other system may write here, ever. THE CHARTER GUARD (plan §C P2): Contracts owns ONLY this table + `haul_receipts`; cargo moves only through Trade-Cargo, credits only through Wallet — the generator/accept write NEITHER, deliver moves both ONLY through those systems' own functions. Reads DOWNWARD only: Reference/Config (templates + `market_offers` for live reward pricing — so ECON-SEED feeds it), Map (`locations`/`location_services` for the active-port filter), and Main Ship (resolve/lock/dock, read-only — the RPCs). DARK behind `haul_contracts_enabled=false` (0176; unchanged by 0179) — the generator returns a no-op envelope (gate-first, NEVER a raise — the cron-safety law) and both RPCs reject `haul_contracts_disabled` before any read while false) | 'offered' rows public-read (per-port bulletins — no player data); accepted/owned rows owner-read (`accepted_by = auth.uid()`); no client write path |
| `haul_receipts` | **Haul Contracts** (sole writers = `haul_accept_contract` + `haul_deliver_contract` (0179) — the per-ship idempotent action record, the `salvage_receipts` 0174 shape point-for-point: unique (main_ship_id, request_id), keyed by ship NEVER player_id, replay returns the original envelope verbatim. ONE table serves BOTH actions (an `action` discriminator 'accept'/'deliver') so a ship's request_id namespace is a single idempotency domain — a reused request id replays whatever it originally did (the 0086 trade-receipts semantics: no payload-conflict check). `reward_credits` = credits ACTUALLY moved by the receipted action (0 on accept — the promise lives on the contract row). DARK behind `haul_contracts_enabled=false` — the writers reject before any read while false, so no row can exist today) | owner (owner-read via the owning ship; authenticated, not anon) |
| `exploration_sites` | **Reference/Config** (admin/migration; static hidden world data — NO runtime writer) | **server-only** (RLS with no client policy/grant: site coordinates are never client-readable before discovery) |
| `exploration_discoveries` | **Exploration** (ONE owner system, two writer fns: `exploration_scan` inserts · `process_exploration_securing` sets `secured_at`; DARK behind `exploration_enabled=false`) | owner |
| `mining_fields` | **Reference/Config** (admin/migration; static hidden world data — NO runtime writer) | **server-only** (RLS with no client policy/grant: field coordinates/composition are never client-readable before extraction) |
| `mining_extractions` | **Mining** (ONE owner system, two writer fns: `mining_extract` (0104) inserts · `process_mining_securing` (0105) sets `secured_at`; DARK behind `mining_enabled=false` — the insert writer rejects before any read while false, so no row can exist today) | owner |
| `mining_field_state` | **World State** (WORLD-BALANCE-P19, 0137 — sole writer = the `worldstate_tick` regen pass + `worldstate_deplete_field`; the per-field mining reserve. Rows created LAZILY on first depletion (no row = full 1.0); Mining reads it DOWNWARD via `worldstate_field_remaining` and drives depletion via `worldstate_deplete_field` — never a second writer. DARK/no-op while `world_balance_enabled=false`) | **server-only** (RLS with no client policy/grant — internal world-state, the `mining_fields` posture) |
| `hull_build_recipes` | **Reference/Config** (admin/migration; the SHIPYARD-0 per-hull build-recipe header (0185) — MIGRATION-SEEDED ONLY, NO runtime writer, ever — the `market_offers` 0085/0173 / `module_recipe_ingredients` 0107 posture. One implicit recipe per hull: `credits_cost` (>= 0) + `build_seconds` (> 0) + the T2+ progression gates `required_hull_type_id` (FK `main_ship_hull_types`, NULL = ungated) / `required_captain_level` (>= 1, NULL = ungated — honest: captain levels are dark); the T1 seeds (`bulk_hauler`/`strike_corvette`, 400 credits / 3600s) carry both gates NULL. Now read DOWNWARD by its FIRST live reader — SHIPYARD-1's `production_start_hull_build` (0188: price/build-time/gate source; spends ONLY via `Inventory.inventory_spend` + `Wallet.wallet_debit` on the reused M4.5 `build_orders` queue — never its own inventory/wallet writes). DARK behind `shipyard_enabled=false` (0185/0188)) | public read-only (RLS public-read policy, no client write path) |
| `hull_recipe_ingredients` | **Reference/Config** (admin/migration; the SHIPYARD-0 normalized per-hull ingredient rows (0185) — MIGRATION-SEEDED ONLY, NO runtime writer, ever. FK to `hull_build_recipes` + `item_types`, `qty > 0`, PK (hull_type_id, item_id) — the exact `module_recipe_ingredients` (0107) / `captain_recipe_ingredients` (0125) normalized-recipe posture; every seeded ingredient is drop-grounded (F4, self-asserted at 0185): ore/crystal = mining-field bundles (0103), scrap/pirate_alloy/weapon_parts/engine_parts = `pirate_loot_for_wave` (head now 0185), `blueprint_fragment` = the 0098 exploration one-shot + the 0185 config-gated w≥8 combat faucet (`blueprint_fragment_drop_rate` seeded '0' — inert). Same consumer as the header table — read DOWNWARD by `production_start_hull_build` (0188, the ingredient bill; quantities are integral by the 0188 apply-time assert, the spend-cast law). DARK behind `shipyard_enabled=false` (0185/0188)) | public read-only (RLS public-read policy, no client write path) |
| `hull_build_receipts` | **Production** (sole writer = private `production_start_hull_build` via the public `start_hull_build` wrapper (0188) — the `module_craft_receipts` (0109) / `captain_recruit_receipts` (0126) per-player idempotency ledger mirrored point-for-point: unique (player_id, request_id) (uuid request ids, the 0174/0179 idiom), a replayed order returns the ORIGINAL success envelope rebuilt verbatim from the receipt (no payload-conflict check); records order_id + credits_spent + the exact `ingredients_json` bill. LIFETIME (review H1, 2026-07-13): receipts OUTLIVE their runtime order rows — the 0047 reaper deletes terminal `build_orders` >30d, so `order_id` is nullable with FK ON DELETE **SET NULL** (never cascade: a cascade would let the reaper silently expire the replay guarantee — a stale retry would place a second full-price order — and destroy the audit bill); replay is guaranteed by the receipt row ALONE (the writer's replay step reads only this table), `player_id` cascade still covers account deletion; pinned by the 0188 confdeltype self-assert + the `SHIPYARD_PASS_RETENTION` proof block (the REAL reaper run in-proof). DARK behind `shipyard_enabled=false` — the writer rejects before any read while false, so no row can exist today) | owner (owner-read; authenticated, not anon) |
| `module_types`, `module_recipe_ingredients` | **Modules** (catalog/config — seeded by migration only, NO runtime writer; `module_types` also carries the Phase-14 fitting catalog columns `slot_cost` + `stats_json` (0111) — same migration-seeded posture, consumer = the Phase 14 fitting adapter (later slice), nothing reads them yet) | public read-only |
| `module_instances` | **Modules** (sole writer = `modules_mint_instance(player, module_type, key)` (0108) — idempotent by the NOT NULL UNIQUE `mint_key`, service_role-only internal leaf; individual instance rows, never counts; called today ONLY by Production's craft command `production_craft_module` (0109) — every producer, any future `build_orders` completion included, must mint through it) | owner |
| `module_craft_receipts` | **Production** (sole writer = private `production_craft_module` via the public `craft_module` wrapper (0109); per-player idempotency ledger, unique (player_id, request_id) — a replayed craft returns the original envelope verbatim; DARK behind `module_crafting_enabled=false` — the writer rejects before any read while false, so no row can exist today) | owner |
| `captain_recruit_receipts` | **Production** (sole writer = private `production_recruit_captain` via the public `recruit_captain` wrapper (0126) — the 0109 `module_craft_receipts` analogue mirrored point-for-point; per-player idempotency ledger, unique (player_id, request_id) — a replayed recruit returns the original envelope verbatim; DARK behind `captain_progression_enabled=false` — the writer rejects before any read while false, so no row can exist today) | owner |
| `ship_module_fittings` | **Fitting** (sole writer = `fitting_apply(player, module_instance, ship|null)` (0112) — FIT and UNFIT both go through THE ONE writer (one sole writer per table covers ALL mutations), service_role-only internal; enforces the STRUCTURAL invariants itself: module+ship ownership, one-ship-per-module (the `module_instance_id` PK), and the Σ `slot_cost` ≤ `module_slots` hard cap (reject, never clamp — the 0044 idiom); called today ONLY by Fitting's own command `fitting_execute_command` (0113), which is DARK behind `module_fitting_enabled=false` (0111)) | owner |
| `module_fitting_receipts` | **Fitting** (sole writer = private `fitting_execute_command` via the public `fit_module_to_ship` / `unfit_module_from_ship` wrappers (0113) — ONE private command handles BOTH actions precisely so this table keeps ONE writer; per-player idempotency ledger, PK (player_id, request_id) — a replayed command returns the receipt's stored `result_json` envelope verbatim (trade semantics, no payload-conflict check); only SUCCESSFUL mutations write a receipt; DARK behind `module_fitting_enabled=false` — the command rejects before any read while false, so no row can exist today) | owner |
| `captain_types` | **Captain** (catalog/config — seeded by migration only, NO runtime writer; carries the Phase-15 mechanism inputs `specialization` (CHECK-constrained tradeoff archetype — the module `slot_type` CASE analogue) + `stats_json` in the ONE shared stat vocabulary (0117) — same migration-seeded posture as `module_types`, read today by the Phase-15 stats-adapter `calculate_expedition_stats` (0122 — `specialization`/`stats_json` feed the headcount-capped captain block), the read surface `get_my_captain_instances`/`get_my_ship_captains` (0123), and the `captains_mint_instance`/`production_recruit_captain` type-existence checks (0118/0126)) | public read-only |
| `captain_recipe_ingredients` | **Captain** (catalog/config — seeded by migration only, NO runtime writer; the normalized recruit-recipe config (0125) — one implicit recipe per `captain_type` = its ingredient rows, FK to `captain_types` + `item_types`, items-only qty (NO metal/credits/Base/Wallet edge), `captain_memory_shard` the shared gating progression ingredient — the exact `module_recipe_ingredients` (Modules) posture. Read DOWNWARD today by its consumer, the Phase-16 **Production**-owned recruit command `production_recruit_captain` (0126), for the recruit cost) | public read-only |
| `captain_instances` | **Captain** (sole writer = `captains_mint_instance(player, captain_type, key)` (0118) — idempotent by the NOT NULL UNIQUE `mint_key`, service_role-only internal leaf; individual instance rows, never counts; its ONE caller is now Production's recruit command `production_recruit_captain` (0126 — mint key `recruit:<player>:<request_id>`); any future acquisition path must mint through this leaf, never a second insert path; DARK behind `captain_progression_enabled=false` (0124) — the recruit command rejects before any read while false, so no row can exist today. **CAPXP (0177): the table gains additive `xp`/`level` columns whose SOLE writer is `captain_xp_accrue()`** — disjoint writers by COLUMN on one table: the mint leaf owns row creation (new rows ride the defaults xp 0 / level 1 — the mint and `captain_assign_apply` NEVER touch xp/level), the accrual owns progression; `level` is maintained inline as `1 + floor(sqrt(xp / 100))` [D proposed curve] and is READ BY NOTHING until the C2-2 adapter parity delta; xp moves ONLY in the cron fold over FINALIZED `reward_grants` (never mid-tick by Combat — the P5 charter guard); DARK behind `captain_growth_enabled=false` (0177)) | owner |
| `ship_captain_assignments` | **Captain** (sole writer = `captain_assign_apply(player, captain_instance, ship|null)` (0119) — ASSIGN and UNASSIGN both go through THE ONE writer (one sole writer per table covers ALL mutations), service_role-only internal; enforces the STRUCTURAL invariants itself: captain+ship ownership (the ship read by the (main_ship_id, player_id) PAIR — the 0079 multi-ship posture; owner-consistency: stored player = captain owner = ship owner, so the adapter (0122) joins without a player filter — the 0115:47–50 guarantee), one-ship-per-captain (the `captain_instance_id` PK + a truthful `already_assigned` naming the current ship), and the HEADCOUNT hard cap `count(*) < captain_slots` (reject, never clamp — count, not Σ slot_cost, because one captain = one slot per the 0117 locked decision); exception-style errors (the 0039/0108 leaf idiom — a locked, documented deviation from `fitting_apply`'s envelopes; the command `captain_execute_command` translates them into client envelopes); the settled-SAFE game rule (the 0114 layer) lives in the command `captain_execute_command` (0121 — the 0114 layer split, exactly as P14 split 0112 from 0114); called today ONLY by that command (0120/0121), DARK behind `captain_assignment_enabled=false`, no row can exist today) | owner |
| `captain_assignment_receipts` | **Captain** (sole writer = private `captain_execute_command` via the public `assign_captain_to_ship` / `unassign_captain_from_ship` wrappers (0120) — ONE private command handles BOTH actions precisely so this table keeps ONE writer; per-player idempotency ledger, PK (player_id, request_id) — captains are non-spatial, the 0113 player-scoped keying — a replayed command returns the receipt's stored `result_json` envelope verbatim (trade semantics, no payload-conflict check); only SUCCESSFUL mutations write a receipt; DARK behind `captain_assignment_enabled=false` — the command rejects before any read while false, so no row can exist today) | owner |
| `captain_counted_grants` | **Captain** (sole writer = `captain_xp_accrue()` (0177) — the SECURITY DEFINER **service-role-only** cron accrual (jobname `captain-xp-accrue`, every 5 min; gate-first on `captain_growth_enabled` → a no-op `{ok:false, code:'feature_disabled'}` envelope while dark, NEVER a raise — the cron-safety law). The per-(grant_id, captain_instance_id) CONSUMPTION LEDGER making captain-XP accrual commit-safe + exactly-once — the `ranking_counted_grants` (0144/0145) idiom with two captain-domain deviations: (1) keyed per (grant, captain) — `unique nulls not distinct (grant_id, captain_instance_id)` — because ONE grant can feed MULTIPLE captains on one sortie manifest; (2) a NULL-captain SENTINEL row marks a grant consumed WITH NO CREDIT (no derivable ship — legacy unit fleets, retention-cleaned encounters, null-scanner discoveries, any future 'trade' grants — or a linked ship with no assigned captains), so every grant is examined EXACTLY ONCE ever: bounded scans, and NO retroactive XP backfill to later-assigned captains. Ship linkage per source (the 0177 header finding): combat = encounter → fleet → `group_sortie_members` manifest ∪ the `fleets.main_ship_id` solo tag; exploration = `exploration_discoveries.main_ship_id` (nullable); mining = `mining_extractions.main_ship_id` (nullable — 0103 ON DELETE SET NULL; destroyed-ship extractions consume as sentinels). XP goes to captains assigned AT ACCRUAL TIME (`ship_captain_assignments` — current-assignment semantic, documented honestly: captain-at-sortie-time is recorded nowhere; a future D-family manifest extension could snapshot it), flat per-source knobs `captain_xp_per_{combat,exploration,mining}_grant` (10/6/4 seeds, [D] owner-tunable). Proven by the CAPXP block of `team-command-proof`. DARK behind `captain_growth_enabled=false` (0177)) | **server-only** (RLS with no client policy/grant — internal accrual bookkeeping; the `ranking_counted_grants` 0144 posture; C2-3 will read xp off `captain_instances`, never this ledger) |
| `ranking_seasons` | **Ranking** (sole writer = `ranking_season_open(cadence, starts_at, ends_at, label)` (0129) — the concrete season-lifecycle writer: PRIVATE, `SECURITY DEFINER`, **service-role-only** (no player wrapper — season management is a server/cron/admin op), DARK-gated on `ranking_enabled` FIRST; rows are runtime-created, NOT migration-seeded; idempotent on the NATURAL KEY (cadence, starts_at) via the 0129 unique index (replay returns the existing window verbatim, NO status churn); per-cadence advisory lock serializes concurrent opens. Opening a NEW active window CLOSES the prior active one of the same cadence (status 'active'→'closed', making room for the partial unique active index) — it NEVER deletes it, so the standings accrued under the closed season_id remain intact (reset by season, NOT deletion — ROADMAP :92); a raced unique-index trip surfaces a clean `{ok:false, code:'conflict'}`. The Ranking-owned root table (0127): a season is a named scoring WINDOW per cadence (`weekly`/`monthly` may run concurrently, independent leaderboards), partial unique index = AT MOST ONE `active` season per cadence. Ranking is a READ-ONLY downward leaf consumer: in later slices it reads `reward_grants` DOWNWARD (the finalized-event source; UNIQUE (source_type, source_id)) and writes only its own tables — no writer to `reward_grants` or any activity table, ever. DARK behind `ranking_enabled=false` (0127)) | public read-only |
| `ranking_standings` | **Ranking** (sole writer = `ranking_accrue_standings()` (0130) — PRIVATE, `SECURITY DEFINER`, **service-role-only** (no player wrapper — accrual is a server/cron/admin op), DARK-gated on `ranking_enabled` FIRST; the per-(season_id, player_id, dimension) leaderboard score row (0128). It reads `reward_grants` DOWNWARD and folds each grant into the matching row via ONE statement that first marks the grant in the `ranking_counted_grants` ledger (0144) then upserts the delta, COMMIT-SAFELY (0145) by a per-(season, grant) ANTI-JOIN against that ledger (grants not yet marked) — NOT the old `granted_at > last_counted_at` timestamp watermark, so a late-committing grant is never skipped and re-runs are a no-op; the 0100/0105 `secured_at` consumption-marker analogue. `last_counted_at` is still written (max granted_at counted) but is now INFORMATIONAL only, never read back as a cursor. Per-event value = the single-source `ranking_score_delta(rewards)` helper (metal + item quantities). `dimension` is EXACTLY the `reward_grants.source_type` domain — the 0096 closed set `('combat','trade','exploration','mining')` — mapped 1:1 with NO translation (folds a grant into the row of its own `source_type`; out-of-domain sources are skipped). ONE row per (season, player, dimension); the OVERALL ranking is DERIVED at read time (sum across dimensions), NEVER a stored denormalized row (no second write path to sync). Leaderboard index `(season_id, dimension, score desc)`. Scores reset BY SEASON (a new `season_id` scopes a fresh standings set; a CLOSED season stops accruing but KEEPS its rows — old rows and the `reward_grants` behind them are never deleted). DARK behind `ranking_enabled=false` (0127); cron-driven every 5 minutes (`ranking-accrue-standings`, 0147) — a dark-gated no-op while `ranking_enabled='false'`, accruing only once the human flips the flag) | public read-only |
| `ranking_counted_grants` | **Ranking** (sole writer = `ranking_accrue_standings()` (0145 — the commit-safe accrual rewrite that inserts here; the slice-A SCHEMA is 0144) — the per-(season_id, grant_id) CONSUMPTION LEDGER that makes accrual COMMIT-SAFE: it records EXACTLY ONCE that a given `reward_grants` row has been folded into a given active season. Replaces the commit-UNSAFE `ranking_standings.last_counted_at` timestamp high-water cursor (0128/0130), which permanently SKIPS any grant that COMMITS after an accrual run advanced the watermark past its transaction-start `granted_at` (`reward_grants.granted_at` defaults to the inserting txn's START time, 0015; visibility is at COMMIT). The accrual (0145) selects grants by ANTI-JOIN against this table (grants not yet marked for the season) instead of by a `granted_at` comparison — commit-safe by construction (a late-committing grant is simply absent and picked up next run, regardless of ordering), exactly-once (`unique (season_id, grant_id)`), idempotent (the 0100/0105 `secured_at` per-row consumption-marker idiom, in its own ledger; one grant folds once per active season it belongs to). `dimension` reuses the IDENTICAL 0128 closed-set CHECK `('combat','trade','exploration','mining')`; `granted_at` is an informational snapshot, NOT a cursor. Sole writer realized in 0145 (slice B). PERMANENT correctness structure, not a shim. DARK behind `ranking_enabled=false` (0127)) | **server-only** (RLS with no client policy/grant — internal accrual bookkeeping, never client-facing leaderboard data; the `mining_fields`/securing-table posture, NOT the `ranking_standings` public-read posture) |
| `location_investments` | **Location Investment** (sole writer = the private `location_investment_invest(player, ship, amount, request_id)` (0133) via the public `invest_in_location` wrapper — `SECURITY DEFINER`, service-role/internal, client-revoked; the Phase-18 root table (0132): an APPEND-ONLY, monotonic per-contribution ledger — `investment_id` PK, `player_id`→`auth.users`, `location_id`→`locations` (Map target), `amount` numeric CHECK (>0), `invested_at`, `request_id` with unique (player_id, request_id) for per-player idempotency (the 0109 `module_craft_receipts` player-scoped keying, NOT ship-scoped; the writer's uuid `request_id` param bridges to this text column via `::text`, 0133). The LEDGER ROW IS THE RECEIPT — replayed (player, request_id) returns the original envelope verbatim (0089/0109 trade-receipt semantics), serialized by a per-player advisory lock before the replay check. PERSISTENT STATE (a location's development) = the all-time SUM per `location_id`, DERIVED from the ledger (no denormalized aggregate — the 0131 "derived, never stored" stance); SEASONAL SCORE = the SUM of a player's contributions within a config-derived season WINDOW (NO season table, NO season-open writer — does NOT duplicate Ranking's `ranking_season_open` machinery; reset-by-season = a new window over the untouched ledger, NEVER deletion). NO INFINITE EXPLOIT: investment is a strict ONE-WAY SINK (`amount > 0`, credits in via `wallet_debit`, NO withdrawal/NO payout). DARK behind `location_investment_enabled=false` (0132) — the wrapper AND the writer reject-before-any-read while false; no read surface exists yet) | owner |
| `world_events` | **World Events** (PHASE20-POLISH — sole writer = `world_events_publish` (insert; idempotent) + `world_events_set_active` (retire/reactivate — flips `is_active`, NEVER deletes) (0140; both SECURITY DEFINER, **service-role only**, client-revoked). The Phase-20 root table (0139): presentational, timed world happenings (`event_type` ∈ `notice`/`world_state`/`seasonal`; a polymorphic `scope` ∈ `global`/`zone`/`location` with a CHECK enforcing the scope↔target invariant — `zone_id`→`zones`, `location_id`→`locations`, Map read-only FK targets; `title`/`body`/`severity`/`is_active`/`starts_at`/`ends_at`; plus the nullable-unique `dedup_key` (0140 — non-null = exactly-once publish via a partial unique index; NULL = ad-hoc non-deduplicated)) the map/dashboard will READ via a later flag-gated read RPC. A NEW downward-LEAF system: it writes ONLY `world_events` and NEVER `zone_state`/`location_state` (World State's tables — NOT a second writer), `fleets`/`combat_*`/`reward_grants` or any other system's table, and grants NO rewards (the one-directional pipeline law). It only READS the static Map (`zones`/`locations`) to validate a supplied FK target. DARK behind `phase20_polish_enabled=false` (0139) — both writers reject-before-any-read (no-op) while false; read via `get_world_events` (0141). **EV-1 (0182):** `worldstate_tick` is now the first server-side CALLER of `world_events_publish` (STATE-detected threshold narration: pressure high/eased / depletion warnings / drift extremes, dedup-keyed per UTC day; it also READS `world_events` rows read-only for the eased noise-suppression) — the two 0140 functions remain the ONLY insert/retire paths (the tick NEVER inserts `world_events` directly; pinned by the 0182 self-assert)) | **server-only** (RLS with no client policy/grant — the `mining_fields`/`market_offers` posture; the only client path is the flag-gated read RPC `get_world_events`, 0141) |
| `ui_asset_catalog` | **Reference/Config** (admin/migration; PHASE20-POLISH UI asset-key vocabulary, 0142 — portraits + icons in ONE table discriminated by `asset_kind`; SEED-MIGRATION ONLY, NO runtime writer (like the static Map / `trade_goods` / `market_offers`); no sole-writer function, no second writer anywhere. Server rows reference a stable `asset_key`; the image FILES live in the frontend (`asset_ref` = the identifier the client resolves). References nothing → a pure static leaf. Read via the flag-gated `get_ui_asset_catalog` (0142)) | **server-only** (RLS with no client policy/grant — the `mining_fields`/`world_events` posture; the only client path is the flag-gated read RPC `get_ui_asset_catalog`, authenticated) |
| `ship_trait_types` | **Reference/Config** (admin/migration; the SHIP-SOUL trait catalog, 0186 — MIGRATION-SEEDED ONLY, NO runtime writer, ever — the `captain_types` 0117 posture verbatim. 8 birthmark traits carrying `stats_json` in the ONE shared input vocabulary (attack/defense/repair/cargo/scan/mining/evasion + speed_mult_bonus — the 0180 adapter's module-read key set, self-assert-pinned at seed time against the hardcoded vocabulary; NOT prosrc-pinned because the traits ADAPTER FOLD lands only in SOUL-1 — nothing reads `stats_json` yet) + `hp_mult` (>= 1.0 by CHECK — a trait never lowers hp; `veteran_frame` 1.08 the sole carrier, applied ONCE at roll time by the roll writer, never re-derived). Read today ONLY by `soul_roll_traits_for_ship` (the catalog-count/order + hp_mult inputs of the deterministic roll)) | public read-only (RLS public-read policy, no client write path) |
| `main_ship_traits` | **Ship Soul** (sole writer = `soul_roll_traits_for_ship(main_ship_id)` (0186) — SECURITY DEFINER, **service-role-only**, gate-first on `ship_traits_enabled` (reject-before-any-read envelope while false). A ship's two rolled traits are a PURE FUNCTION of its `main_ship_id` (the 0041 determinism law via the 0176 pure-hash technique: `hashtextextended` over the `:soul:<slot>` salts → `((h % n + n) % n)` into the `trait_type_id` order under the collate "C" byte-order pin (column + ORDER BY — the DB default collation can never re-order the derivation); slot 2 re-salts deterministically until distinct — NO random()/setseed, prosrc-pinned by the 0186 self-assert) and are **INSERT-ONLY IMMUTABLE**: `on conflict (main_ship_id, slot) do nothing` makes a re-call a zero-insert replay (a re-roll cannot exist), the writer's body contains no UPDATE/DELETE against the table (prosrc-pinned), clients hold ZERO write privilege (default grants stripped, owner-read SELECT only), and `unique (main_ship_id, trait_type_id)` makes slot-distinctness a schema fact. `hp_mult` application (max_hp := round(base × mult), hp scaled proportionally, `>`-guarded monotonic — the 0171 never-lower posture) fires ONLY when the rows actually landed this call, so a replay can never re-raise hp. DARK behind `ship_traits_enabled=false` (0186) AND doubly dark: NOTHING calls the roll fn — SOUL-1 owns the commission hook + adapter fold, so no row can exist today. NO §2 Ship-Soul system row yet (the 0111/0117 deferral posture: the system isn't live until SOUL-1 wires its callers)) | owner (owner-read via the owning ship — the 0074 `ship_cargo_lots` EXISTS idiom; authenticated, not anon) |

**Source of truth for active combat** = `combat_encounters` + `combat_rounds` +
`fleet_units` + `location_presence`. **Never** `combat_reports` (history only).

---

## 2. Per-system contract (owns / exposes / forbidden)

| System | Owns (writes) | Public functions (only way in) | Must NOT |
|---|---|---|---|
| **Map** | sectors, zones, locations | `get_world_map()`, `get_location_detail()` | move fleets · combat · rewards · change unit counts · create presence |
| **World State** | zone_state, location_state | `worldstate_register_presence(loc)`, `worldstate_unregister_presence(loc)`, `worldstate_tick()` *(the 60s heartbeat; sole writer of `location_state`/`zone_state`. Pressure decays toward a TARGET: baseline PLUS a **pirate-pressure** danger term (WORLD-BALANCE-P19, 0135). The danger term is `0` — target = baseline, the pre-Phase-19 decay-toward-baseline verbatim — UNLESS `world_balance_enabled=true`, when it = `count(recent 'defeat' combat_reports at this location within world_balance_defeat_window_seconds) * worldstate_pressure_defeat_increase` (the 0032 key, reused). This is the wired form of the long-standing `defeat_pressure` seam: it READS `combat_reports` DOWNWARD (history, read-only) — the ONLY cross-system access, acyclic (Report writes only `combat_reports` and calls nothing). **DARK/no-op while `world_balance_enabled='false'`** (0135): the danger term is 0 and `combat_reports` is not even read, so a dark tick's output is byte-identical to pre-slice. Bounded by the SAME cap — pressure never exceeds `worldstate_pressure_max`. **PRICE DRIFT (WORLD-BALANCE-P19, 0136):** the tick is ALSO the sole writer of the new World-State-owned column `location_state.price_multiplier` (numeric, default 1.0, CHECK > 0) — it breathes toward a bounded danger premium (`target = 1.0 + world_balance_price_pressure_coeff × clamp((pressure−baseline)/(max−baseline),0,1)`, nudged by `world_balance_price_drift_rate`, clamped to `[world_balance_price_multiplier_min, world_balance_price_multiplier_max]`), reusing the existing baseline/max config. DARK/no-op while `world_balance_enabled='false'`: the tick leaves the column untouched (stays 1.0) and none of the premium math runs. Exposes ONE read helper `worldstate_current_price_multiplier(loc)` (internal/service-role) — returns 1.0 while dark regardless of the stored value, else the row's `price_multiplier` (1.0 if no row); read DOWNWARD by Trade Market, never a write path). **FIELD DEPLETION (WORLD-BALANCE-P19, 0137):** the tick is ALSO the sole writer (via its gated regen pass) of the World-State-owned table `mining_field_state` (`field_id` PK → `mining_fields`, `reserve_fraction` numeric default 1.0 CHECK [0,1]) — rows created LAZILY (no row = full 1.0); regen nudges each `reserve_fraction` toward 1.0 by `world_balance_field_regen_rate`/tick, clamped ≤ 1.0. Two more functions (internal/service-role, both flag-gated fail-closed): `worldstate_field_remaining(field)` (→ 1.0 while dark or no row, else `reserve_fraction`; read DOWNWARD by Mining) and `worldstate_deplete_field(field)` (no-op while dark; else upserts `reserve_fraction` down by `world_balance_field_depletion_per_extract`, hard-floored at `world_balance_field_reserve_min` — NO-SOFTLOCK; THE sole reserve write on extraction, called DOWNWARD by Mining). DARK/no-op while `world_balance_enabled='false'`: the tick never touches `mining_field_state` and both functions self-gate. **EVENT NARRATION (EV-1, 0182):** the tick also NARRATES threshold happenings by CALLING World Events' own sole writer `world_events_publish` DOWNWARD — ALL STATE-detected (never edge: a failed publish's condition still holds next minute, so the retry is genuine): `pressure_high` warning/critical while pressure sits at/above `event_pressure_high_threshold` (a parked-high location re-announces once per UTC day — intended pressure-nagging [D]) + `pressure_eased` (the exact complement, noise-suppressed unless TODAY's `pressure_high` was announced — a read-only lookup of the tick's OWN published `world_events` rows, never a write; a perpetually-calm location never "eases"), depletion warnings (`field_depleting` while a post-regen reserve sits below `event_depletion_warn_fraction`), drift extremes (`price_surge`/`price_crash` outside `event_drift_extreme_band_low/_high`), each dedup-keyed per (subject, UTC day). KNOB SAFETY: the four EV-1 knobs are read ONLY when `world_balance_enabled` (a dark tick reads no EV-1 config) and the reads are guarded (bad cast / NaN → seeded default + WARNING — an owner-tunable typo can never kill the live heartbeat). NARRATE-NEVER-MUTATE: publication is presentational only — never a write into any World-State (or other) table, and NEVER a direct `world_events` insert. Every publication sits inside the `world_balance_enabled` gate AND `world_events_publish` self-gates on `phase20_polish_enabled` (double-dark: zero events unless BOTH are true), and EVERY publication is its OWN begin/exception subtransaction (per location-event, per depleting field; `query_canceled` re-raised, everything else → WARNING) so a publish error NEVER aborts the 60s heartbeat and never takes a sibling publication down)* | touch fleets/combat/rewards · **write** `combat_reports` or any Report/Combat table (reads `combat_reports` DOWNWARD, read-only) · add a runtime writer to `market_offers` or `mining_fields` (drift/depletion live on World-State-owned tables, composed at read/extraction time) · **write** `world_events` directly (narration goes ONLY through `world_events_publish` — World Events' sole writer, EV-1 0182) |
| **Base** | bases, base_units, base_resources | `initialize_new_player()`, `base_reserve_units(base,units)`, `base_merge_units(base,units)`, `base_add_resources(base,rewards)` *(only Reward calls this)*, `base_spend_resources(base,resource,amount)` *(only Production calls this)* | movement/combat logic |
| **Fleet** | fleets, fleet_units | `fleet_create(...)`, `fleet_set_moving/present/returning/complete/destroy(...)` *(state-guarded)*, `fleet_combat_stats(fleet)`, `fleet_sync_quantities(fleet,counts)`, `fleet_get_power(fleet)` | write base_/combat_/movement tables |
| **Movement** | fleet_movements | `movement_create(fleet,origin,target,mission)`, `movement_attach_cargo(movement,source,bundle,source_type='combat')` *(internal; attaches the pending `{metal?, items[]}` bundle + its activity-agnostic `reward_source_type` to the return movement — the ONE shared carrier every activity reuses)*, `process_fleet_movements()`, `command_main_ship_stop_transit(fleet)` *(authenticated, 0149 → 0155; gated on the EXISTING `mainship_send_enabled` — HALTS AND HOLDS the caller's in-transit MAIN-SHIP fleet movement in open space (0155 replaced the 0149/0152 return-home: Stop must halt-and-hold, not fly the ship home). Settlement: terminate the SAME `fleet_movements` row `status='cancelled', resolved_at=now()` (a first-class 0007 terminal — an honest halt, not a false `'arrived'`); settle the fleet to the movement-less terminal `status='completed', location_mode='movement', active_movement_id=NULL, current_*=NULL` (the OSN-stop held-fleet shape, 0064:312-317); hold the ship at the interpolated halt point in the ALREADY-LEGAL `(status='stationary', spatial_state='in_space', space_x/y)` representation (0055:143-144/159-161 + 0054; the SAME canonical shape OSN's stop writes, 0064:319-322 — written inline, the shared invariant is the constraint set, not a helper). Idempotent/race-safe BY STATE: it claims the movement row FOR UPDATE in the cron's own lock order and every mutation is guarded `status='moving'`, so a cron-settled/due call is a clean `{stopped:false}` no-op and a due row is LEFT for `process_fleet_movements` — settlement stays single-path; a DUPLICATE stop finds the ship already held → `{stopped:false, reason:'already_held'}`. No reward interaction (main-ship targets are activity 'none'; payload stays '{}'). NO constraint edit, no new table, no flag change, OSN untouched. Slice B adds legacy re-departure (Send) from the held position)*, `movement_settle_arrival(movement)` *(internal, 0151 — THE extracted per-movement settle: the verbatim 0096 loop body (location arrival → arrived + `fleet_set_present` + `presence_create(activity)` — hunt arrivals still route into Combat via activity_start exactly as before; base arrival → arrived + unit merge + `fleet_complete` + the activity-agnostic reward deposit; unknown target → failed) behind a guarded locked re-read (`status='moving' AND due` FOR UPDATE — a no-op re-take for the cron, the authoritative claim for the on-demand caller). BOTH `process_fleet_movements()` (loop body = this helper; scan/locks unchanged, byte-equivalent) and the on-demand RPC call it — one settlement body, ever)*, `command_main_ship_settle_arrival_legacy(fleet?)` *(authenticated, 0151; gated on the EXISTING `mainship_send_enabled` — settles the CALLER'S own due main-ship fleet_movements arrival immediately instead of waiting the 30s cron. Fleet resolution = explicit owned id or the sole in-flight main-ship fleet (fail-closed on ambiguity); main-ship-only (the 0050 return predicate); claims the movement FOR UPDATE SKIP LOCKED in the cron's own lock order (contention → `busy`, the cron wins, never blocks); NON-COMBAT SCOPED — refuses any `activity_type<>'none'` location target (`combat_target_unsupported`) so the on-demand path can never drive combat init (structurally unreachable for main-ship fleets anyway, 0050:104/0053:71); idempotent by state via the helper's `status='moving' AND due` guard (cron race → exactly-once, loser no-ops `already_settled`; `not_due`/`no_active_movement` safe no-ops); no reward re-entry (main-ship payload stays '{}'))*. **`fleet_movements.origin_type` domain (0156)** = `('base','location','zone','space')`: `'space'` = departed from a held open-space coordinate (`origin_*_id` all NULL, `origin_x/y` = the held point). `origin_type` is PROVENANCE METADATA ONLY — settlement branches on `target_type`, nothing reads `origin_type` — so this is additive. The send-surface RPC `move_main_ship_to_location(fleet,location)` (a Main-Ship send entry that CALLS `movement_create`; authenticated, gated `mainship_send_enabled`) now departs from EITHER validated state — PRESENT at a location (`origin_type='location'`) OR HELD in open space (`origin_type='space'`, the 0155 Stop-hold shape) — same one legacy path, same non-combat destination guard; Movement stays the sole writer of `fleet_movements` (no new writer/table) | combat math · spawn pirates · rewards · unit losses · victory/defeat |
| **Presence** | location_presence | `presence_create(fleet,loc,activity)`, `presence_request_leave(presence)`, `presence_complete/destroy/expire(...)` | combat damage · resource writes · map writes |
| **Activity** *(no table — router)* | — | `activity_start(presence,type)` → `hunt_pirates`→Combat, `none`→no-op | own gameplay state; MVP only dispatches |
| **Combat** | combat_encounters, combat_rounds | `combat_create_encounter(presence)`, `process_combat_ticks()`, `combat_set_retreating(enc)` | move fleets (except request return via Movement) · edit map · add resources directly · mining/trade/captains |
| **Reward** | reward_grants | `reward_grant(source_type,source_id,player,base,bundle)` *(idempotent; splits the `{metal?, items[]}` bundle: metal → `Base.base_add_resources` (sole caller), items → `Inventory.inventory_deposit`)* | combat math · movement · write inventory/base tables directly |
| **Report** | combat_reports | `report_create(encounter)` *(idempotent)*, `get_combat_reports()` | **any** gameplay mutation · be a source of truth for active state |
| **Production** (Training) | build_orders, module_craft_receipts *(0109)*, captain_recruit_receipts *(0126)*, hull_build_receipts *(0188)* | `train_units(base,unit,qty)` *(player)*, `process_build_queue()` *(cron)*, `production_create_order/complete_order(...)` *(internal)*, `craft_module(request_id, module_type)` *(authenticated; **DARK** behind `module_crafting_enabled=false` (0107) — gate checked FIRST, reject-before-any-read, in both layers)* → private `production_craft_module(player, module_type, request_id)` *(service_role/internal; the 0099/0104 two-layer idiom. Idempotent on (player, request_id): a per-player advisory lock (the 0078 commission idiom) serializes crafts BEFORE the replay check, and a replayed request returns the ORIGINAL success envelope rebuilt verbatim from the receipt — the 0089/0095 trade-receipts semantics, deliberately NO payload-conflict check. Cost is ITEMS-ONLY (the recipe rows; no metal, no credits — no Base/Wallet edge): balances pre-checked via `Inventory.inventory_get_balance` (friendly `insufficient_items` envelope, nothing spent), then one transaction fans out one-directionally DOWNWARD to `Inventory.inventory_spend` per ingredient (its exceptions roll back the whole craft — a failed craft writes NO receipt) + `Modules.modules_mint_instance` (mint key `craft:<player>:<request_id>` per the 0108 key contract) — ONE craft = ONE instance — then writes its own receipt. Edges all DOWNWARD (Production → Inventory · Modules · Reference/Config), acyclic)*, plus `recruit_captain(request_id, captain_type)` *(authenticated; **DARK** behind `captain_progression_enabled=false` (0124) — gate checked FIRST, reject-before-any-read, in both layers)* → private `production_recruit_captain(player, captain_type, request_id)` *(service_role/internal; the 0109 `craft_module` analogue mirrored POINT-FOR-POINT. Idempotent on (player, request_id): a per-player advisory lock (`captain_recruit` domain) serializes recruits BEFORE the replay check, and a replayed request returns the ORIGINAL success envelope rebuilt verbatim from the receipt — deliberately NO payload-conflict check. Cost is ITEMS-ONLY (the 0125 `captain_recipe_ingredients` rows; no metal, no credits — no Base/Wallet edge): balances pre-checked via `Inventory.inventory_get_balance` (friendly `insufficient_items` envelope, nothing spent), then one transaction fans out one-directionally DOWNWARD to `Inventory.inventory_spend` per ingredient (its exceptions roll back the whole recruit — a failed recruit writes NO receipt) + `Captain.captains_mint_instance` (mint key `recruit:<player>:<request_id>` per the 0108 key contract — `production_recruit_captain` is now that leaf's FIRST caller) — ONE recruit = ONE instance — then writes its own receipt, reading `Captain.captain_recipe_ingredients` DOWNWARD for the recipe. NEW edges all DOWNWARD (Production → Captain mint + recipe read), reusing the existing Production → Inventory (spend) + Reference/Config (cfg) edges — acyclic, no cycle; Captain stays a pure instance-leaf with NO Captain→Inventory edge)*, plus `start_hull_build(request_id, hull_type)` *(authenticated; **DARK** behind `shipyard_enabled=false` (0185) — gate checked FIRST, reject-before-any-read, in both layers, and while dark the wrapper's answer is IDENTICAL regardless of input — no hull-existence oracle)* → private `production_start_hull_build(player, hull_type, request_id)` *(service_role/internal; the 0109 two-layer idiom on the SHIPYARD-1 ORDER side. Idempotent on (player, request_id): a per-player advisory lock (`hull_build` domain) serializes orders BEFORE the replay check, and a replayed request returns the ORIGINAL success envelope rebuilt verbatim from the receipt — no payload-conflict check. Validation order: gate → request → lock → replay → catalog (unknown_hull vs no_recipe — T0 `starter_frigate` is deliberately recipe-less) → the 0185 progression gates (required hull owned non-destroyed / any owned captain at `required_captain_level` — reads `main_ship_instances` + `captain_instances` DOWNWARD, read-only; both arms dormant on the NULL T1 seeds) → the SHARED M4.5 cap (`max_build_orders` over waiting+active — one queue, one cap with training) → ingredient pre-check via `Inventory.inventory_get_balance` (friendly envelope, nothing spent) → `Wallet.wallet_debit` (the first write; false → friendly `insufficient_credits`, nothing else spent) → `Inventory.inventory_spend` per ingredient (exceptions roll back the WHOLE order, debit included — all-or-nothing; a failed order writes NO receipt) → the `build_orders` hull-row insert ('waiting', quantity 1, NO timestamps — the SHIPYARD-2 engine promotes/delivers) → its own receipt. Deterministic (no random(); `order by item_id`). Edges all DOWNWARD (Production → Reference/Config · Main Ship (read) · Captain (read) · Inventory · Wallet), acyclic)* | write base_units/base_resources directly (spends via `Base.base_spend_resources`, deposits via `Base.base_merge_units`) · **write player_inventory/inventory_ledger/module_instances directly (ingredients ONLY via `Inventory.inventory_spend`; minting ONLY via `Modules.modules_mint_instance`)** · **write captain_instances directly (recruit minting ONLY via `Captain.captains_mint_instance`)** · **write player_wallet directly (credits ONLY via `Wallet.wallet_debit`)** · touch combat/world-state/movement · change reward logic · craft or recruit or order a hull build while the gate is off · promote/deliver a hull order (SHIPYARD-2 owns the engine's hull arm — until then hull rows stay 'waiting' by the pinned unit_types-join invisibility) |
| **Inventory** | player_inventory, inventory_ledger | `inventory_deposit(player,item,qty,key?)` *(idempotent)*, `inventory_spend(player,item,qty)` *(transactional)*, `inventory_get_balance(player,item)` | combat/movement/world-state · be a source of truth for live combat · client writes · touch metal/`base_resources` (metal stays Base-owned) |
| **Main Ship** | main_ship_instances | `ensure_main_ship_for_player(player)` *(idempotent; ensures the first/sole ship)*, `get_main_ship(player)`, `rename_main_ship(player,name)`, `commission_first_main_ship()` / `commission_additional_main_ship()` *(the priced additional-ship path — DARK)* *(all server-only)*, `mainship_resolve_docked_location(ship)` *(shared read-only docked-location helper; internal, called DOWNWARD by the Trade Market RPCs)* — a player MAY own multiple `main_ship_instances` rows (the `player_id` UNIQUE was dropped in 0079); sole-ship is now a runtime shim / dark gate, not a schema constraint | touch fleets (except the sanctioned port-entry commission shim recorded in the §1 `fleets` row: `port_entry_commission_build` fleet-row insert + `normalize_main_ship_dock` dock-state normalize, own-player rows only, with its retirement condition)/combat/movement/production · drive expeditions yet · client writes |
| **Wallet** | player_wallet | `wallet_ensure(player)` *(the ONE shared lazy-ensure + seed: inserts the wallet exactly once with balance = `cfg_num('starting_credits')`; idempotent + unfarmable by the `player_id` conflict)*, `wallet_debit(player,amount)` *(ensure, then atomic conditional debit; false if too poor)*, `wallet_credit(player,amount)` *(ensure, then unconditional add)* — **internal** (no client grant); both writers call `wallet_ensure` (no duplicated ensure block). Called DOWNWARD by Main Ship (additional-ship commission debits `main_ship_price`) and Trade Market (buy debits / sell credits); reads `cfg_num('starting_credits')` from Reference/Config — a DOWNWARD read to an existing leaf. A **downward leaf**: depends on nothing above it → no cycle, no mutual/two-way dependency | write any other system's table · be called cyclically · client writes |
| **Trade Cargo** | ship_cargo_lots | `trade_cargo_add_lot(ship,good,qty,cost,origin)` *(insert)*, `trade_cargo_consume(ship,good,qty)` *(FIFO delete/update; returns consumed cost basis)* — **internal** (no client grant); per-ship, volume-keyed cargo lots hung on a `main_ship_instances` ship. A **leaf** Trade Market depends on downward | write main_ship_instances/player_wallet/trade_receipts · cache volume on the instance · client writes |
| **Trade Market** | trade_receipts, trade_relief_claims | `get_market_offers(ship)` *(read)*, `market_buy(ship,good,qty,req)`, `market_sell(ship,good,qty,req)` *(authenticated; idempotent on (ship,request_id))* — orchestrates buy/sell by fanning out one-directionally DOWNWARD to Wallet (debit/credit) + Trade Cargo (add/consume lots), reading `market_offers` (the Reference/Config price catalog — the source of the BASE offer prices: `get_market_offers` projects the docked station's active offers, `market_buy` takes its `sell_price`, `market_sell` its `buy_price`) + `trade_goods` (good identity/metadata — `unit_volume_m3` for the buy-side volume check) + the docked-location context (via the shared Main-Ship helper `mainship_resolve_docked_location`); writes only its own `trade_receipts`. **PRICE COMPOSITION (WORLD-BALANCE-P19, 0136):** the base `market_offers` price is no longer the final charged price — every price is composed through the ONE shared helper `trade_effective_price(base, loc)` = `greatest(1, round(base × worldstate_current_price_multiplier(loc)))` (integer credits, ≥1 floor). All three functions route through it — `get_market_offers` composes BOTH displayed prices, `market_buy` the charged `sell_price`, `market_sell` the paid `buy_price` — so the DISPLAYED price always equals the CHARGED/PAID price (no drift-vs-transaction exploit), single-sourced in one place. This adds a NEW DOWNWARD edge Trade Market → World State (read `worldstate_current_price_multiplier`) — Trade Market still writes ONLY its own `trade_receipts` and NEVER writes `market_offers` (static) or `location_state` (World-State-owned). While `world_balance_enabled='false'` the multiplier is 1.0, so composed = `round(base)` = the pre-slice integer prices. **DARK**: every trade RPC is server-rejected while `trade_market_enabled=false`. The **no-softlock relief** orchestrator `market_claim_relief(request_id)` *(authenticated; idempotent on (player,request_id))* is sited here too and is the **SOLE writer** of `trade_relief_claims` (per-player idempotent ledger). It grants the relief credit THROUGH `wallet_credit` (so `player_wallet`'s sole-writer invariant holds — relief NEVER writes `player_wallet` directly), and reuses the EXISTING downward fan-out: it reads `player_wallet` directly `FOR UPDATE` (the rock-bottom balance check + per-account lock — a downward read, the write still goes only through Wallet), and reads `ship_cargo_lots` + `main_ship_instances` downward for the cargo check — so it introduces **no new edge and no cycle**, and Wallet stays a downward leaf. **DARK**: gated by `trade_relief_enabled=false`. Tunables (Reference/Config): `relief_credits`, `relief_cooldown_seconds`, `relief_max_lifetime_claims` | write ship_cargo_lots/player_wallet/main_ship_instances directly · second-write any table · run while the gate is off |
| **Salvage Market** | salvage_receipts | `sell_item_at_port(ship, item, qty, req)` *(authenticated; SALVAGE-1, 0174 — the 0090/0138 trade-sell shape on ITEMS: idempotent on (ship, request_id), atomic in one SECURITY DEFINER txn under the per-ship `mainship_space_lock_context`. Reject order (envelopes): `not_authenticated` → `salvage_market_disabled` (gate FIRST, before any read) → `invalid_request`/`invalid_item`/`invalid_quantity` (items are INTEGER quantities — fractional rejects, never rounds) → `ship_not_found` (`mainship_resolve_owned_ship`) → `not_docked` (the ONE shared resolver `mainship_resolve_docked_location`, 0092/0138) → `no_demand` (no active `port_item_demand` row for (port, item)) → `idempotent_replay` → `insufficient_items` (pre-check via `inventory_get_balance`; enforcement stays `inventory_spend`'s own FOR UPDATE — the 0109 crafting posture) → ok. Fan-out is one-directional DOWNWARD: Main Ship (resolve/lock/dock, read-only) → Reference/Config (`port_item_demand`) → **Inventory** (`inventory_spend` — SALVAGE consumes inventory EXACTLY like crafting does; never a direct `player_inventory` write) → **Wallet** (`wallet_credit`) → its own `salvage_receipts`. PIPELINE LAW UNCHANGED: combat → pending → secured → inventory; combat never grants credits directly — selling is a player-initiated docked action. **DARK** behind `salvage_market_enabled=false` (0174); proven by the self-rolling-back `scripts/salvage-market-proof.{sql,sh}` in `trade-v1-proof.yml`)* | write player_inventory/player_wallet/port_item_demand directly · buy items back from players (demand is buy-side only — no pump loop) · second-write any table · sell a progression item (no demand row may ever exist for one) · run while the gate is off |
| **Haul Contracts** | haul_contracts, haul_receipts | `haul_generate_offers()` *(service_role/cron ONLY — never a client RPC; HAUL-1, 0176, re-created 0179 under parity discipline with the ONE marked (a2) hunk. Gate-first on `haul_contracts_enabled` → a no-op `{ok:false, code:'feature_disabled'}` envelope while dark (NEVER a raise — the cron-safety law; every dark hourly firing is an instant no-op). When lit: (a) EXPIRY — 'offered' rows past `expires_at` flip 'expired' (predicate status='offered' ONLY; accepted contracts are never expired); (a2) DEADLINE CANCEL (0179) — 'accepted' rows past `deliver_by` flip 'cancelled' (the SINGLE writer of that transition; no penalty v1 [D]; frees the player's active-cap slot; within-deadline accepted rows untouchable by every pass); (b) GENERATION — per ACTIVE port (status='active' + active docking + an active market) × slot 1..`haul_offers_per_port`, a pure-hash-deterministic (day, port, slot) pick over the weighted `haul_contract_templates` pool, priced off the LIVE `market_offers` rows (`reward = round(base + qty × (dest.buy + premium))` — always modestly beats the same-haul self-trade), inserted `on conflict do nothing` on the natural key (re-runs mint nothing))* · `haul_accept_contract(ship, contract, req)` *(authenticated; HAUL-2, 0179 — a CLAIM: moves NO cargo, NO credits (the player sources the goods themselves — the 0176 reward math prices in the origin buy cost). Reject order (envelopes): `not_authenticated` → `haul_contracts_disabled` (gate FIRST, before any read) → `invalid_request` → `ship_not_found` (`mainship_resolve_owned_ship`) → per-ship `mainship_space_lock_context` → `not_docked` (the ONE resolver 0092/0138) → `contract_not_found` (row FOR UPDATE: missing/terminal/not at THIS port — origin-port accept, the bulletin is port-scoped — or offered-but-past-`expires_at`, fail-closed ahead of the cron) → `already_accepted_other` → `too_many_active` (`haul_max_active_per_player` knob, seeded 3 [D]; the count EXCLUDES the contract being re-accepted so a replay at the cap still works) → `idempotent_replay` → `already_accepted` (held by ME under a fresh request id) → ok: offered→accepted, accepted_by/ship/at + `deliver_by = accepted_at + template duration_seconds` (the 0176 duration reused as the delivery window — the contract's tempo), receipt)* · `haul_deliver_contract(ship, contract, req)` *(authenticated; HAUL-2, 0179 — ANY owned ship may deliver (the contract is player-scoped; `accepted_ship` is provenance only). Reject order: `not_authenticated` → `haul_contracts_disabled` → `invalid_request` → `ship_not_found` → per-ship lock → `not_docked` → `contract_not_found` (row missing) → `idempotent_replay` (BEFORE the state/port/deadline/cargo guards — a successful delivery flips the status and consumes the cargo, so a retry must replay, never bounce; the salvage-template posture) → `contract_not_found` (not 'accepted' by THIS player) → `wrong_port` (docked ≠ dest) → `deadline_passed` (past `deliver_by`; reject-only — the cancel flip stays the generator's (a2)) → `insufficient_cargo` (inline lot-sum pre-check, the market_sell 0090 read idiom) → ok: `trade_cargo_consume` (the ONE FIFO cargo debiter — cost basis consumed and LOST, the reward covers it; reported as `cost_basis_consumed`) + `wallet_credit(player, reward)` + accepted→delivered + receipt, all atomic)* · `get_port_contracts(location)` *(authenticated; HAUL-3, 0181 — the read-only bulletin surface (the 0123 read-surface precedent: recorded here, no §1 matrix change): auth → gate-first `haul_contracts_disabled` BEFORE any row read (the house reject-before-read law — chosen over the RLS direct select 0176 left possible, so the client keeps ONE server-driven visibility signal and an emergency lit→dark flip hides any stale bulletin rows too) → `invalid_location` → {ok, location_id, max_active (the cfg cap surfaced for the client's too_many_active mirror), offered:[this port's status='offered' rows with `expires_at > now()` — the accept RPC's own fail-closed stale-offer posture — ordered by slot, dest display names via a read-only `locations` join], mine:[the caller's status='accepted' contracts at ALL ports with `deliver_by`, soonest first; query-scoped `accepted_by = auth.uid()`, defense in depth over RLS]}. Everything proven by the self-rolling-back `scripts/haul-proof.{sql,sh}` in `trade-v1-proof.yml`)* | write ship_cargo_lots/player_wallet/market_offers/haul_contract_templates directly or any other system's table (cargo only via Trade-Cargo functions, credits only via Wallet) · expire or deadline-cancel an accepted contract anywhere but the generator's passes · mint outside the deterministic natural key · use session RNG (`random()`/`setseed`) — determinism is the contract · move cargo/credits at ACCEPT (a claim is free) · run while the gate is off |

| **Exploration** | exploration_discoveries | `command_exploration_scan(ship,req)` *(authenticated; the ONE client entry — 0083 wrapper idiom: auth → anti-probe flag gate → `mainship_resolve_owned_ship` → delegate)* → private `exploration_scan(player,ship,req)` *(service-role/internal; inserts discoveries — a `not exists` pre-check for the settled duplicate (`already_discovered`) PLUS a `unique_violation` catch (0146) that converts a racing duplicate insert into the SAME clean `already_discovered` result (writing no receipt), guarded by the `unique (player_id, site_id)` constraint — the sole authority for at-most-one discovery per (player, site); the racing path needs two ships of one player, latent behind the dark `mainship_additional_commission_enabled` gate)*, plus `get_my_exploration_discoveries()` *(read-only, dark-gated `exploration_disabled` — 0087 read idiom; the ONLY client path to site data, strictly post-discovery: it joins the caller's OWN discovery rows to the hidden sites, so an undiscovered site stays unreachable by construction)*. **DARK** behind `exploration_enabled=false` (0097): the writer rejects `feature_disabled` BEFORE any other read, and the wrapper gates identically. The writer reuses — never re-invents — the OSN command machinery DOWNWARD: `main_ship_space_command_receipts` idempotency (0055; replay-safe on (ship, request_id)), the S2 canonical lock order (ship → fleet → coordinate movement → presence), `mainship_space_validate_context` (requires settled `in_space`) + `mainship_space_assert_cross_domain_exclusion`, and the pure geometry leaf `osn_distance`. It reads `exploration_sites` DOWNWARD (hidden server-only data; a site is revealed only at the moment of discovery) and ACCRUES the pending bundle on its own state (`pending_bundle_json`, `secured_at` NULL, scanning ship in `main_ship_id`). **Securing (0100):** Exploration's OWN cron `process_exploration_securing()` *(60s; internal/service_role; `FOR UPDATE SKIP LOCKED`)* deposits a pending bundle once the carrying ship (row's ship, else the player's canonical ship) is settled **SAFE** — `spatial_state in ('home','at_location')` per the 0055 state model, never in_transit/in_space/destroyed/legacy — by calling `Reward.grant('exploration', discovery_id, player, base, bundle)` — **THE sole secured-deposit owner, exactly as `process_fleet_movements` calls it for fleet returns** — then sets `secured_at` (idempotency DOUBLE-guarded: `secured_at` + `reward_grants` UNIQUE (source_type, source_id)). `movement_attach_cargo` is the **fleet_movements-domain carrier ONLY** (combat today): it is UNREACHABLE for OSN-native scanning (an in-space ship never traverses `fleet_movements` — cross-domain exclusion; no OSN home leg), which is why Exploration's processor calls the depositor directly. IN-FLIGHT SAFETY (0064 precedent): the processor does NOT check the flag — accrued value is never stranded by an emergency flag-off; inert while scan is dark (no rows can exist). Edges (all DOWNWARD, acyclic): Exploration → OSN geometry/locks (read) · Main Ship (read) · Bases (read: deposit target) · Reward (grant); OSN and the arrival processors never call into Exploration | write exploration_sites (no runtime writer exists) · deposit outside `Reward.grant('exploration', discovery_id, …)` (never write inventory/base tables directly) · write another activity's tables · expose an undiscovered site's coordinates to a client · scan while the gate is off |

| **Mining** | mining_extractions | `command_mining_extract(ship,req)` *(authenticated; the ONE client entry — 0083 wrapper idiom: auth → anti-probe flag gate → `mainship_resolve_owned_ship` → delegate)* → private `mining_extract(player,ship,req)` *(service-role/internal, 0104; the SOLE insert path — REPEATABLE, no `unique (player_id, field_id)`: one row per extraction; pacing = the per-(player, field) cooldown from the latest `created_at` vs `mining_extract_cooldown_seconds` (`cooldown` envelope carries `retry_after_seconds`), plus a per-(player, field) xact-scoped advisory lock `pg_advisory_xact_lock(hashtext('mining_extract'), hashtext(player:field))` (0143, the 0078/0113/0126/0133 idiom) taken right BEFORE the cooldown read as DEFENSE-IN-DEPTH. Reachability (verified 2026-07-05): the `(player, field)` no-double-extract invariant is ALREADY held today by the S2 ship `FOR UPDATE` lock (0056:46) + the cooldown read + the fact that every player holds ≤ 1 main ship — the additional-ship path `commission_additional_main_ship` (0080) is DARK behind `mainship_additional_commission_enabled='false'`, and the `0043` `player_id` UNIQUE was dropped in `0079`, so ≤ 1-ship is a dark-gate/runtime invariant, not a schema constraint. With ≤ 1 ship, two concurrent extracts contend on the SAME ship row and the second is cooldown-rejected. The advisory lock is inert today and becomes LOAD-BEARING only IF/WHEN multi-ship-per-player is activated (that flag flipped), when two ships of one player at one field could otherwise both pass the read-then-insert cooldown check and double-extract. It is a PERMANENT correctness guard — xact-scoped ⇒ auto-released at commit/rollback, no cleanup, no softlock — not a shim (no retirement; the multi-ship activation is its relevance trigger, not a removal condition); target = the NEAREST active field within `mining_extract_radius` via the `osn_distance` leaf)*. **DARK** behind `mining_enabled=false` (0102): the writer rejects `feature_disabled` BEFORE any other read, and the wrapper gates identically. The writer reuses — never re-invents — the OSN command machinery DOWNWARD exactly as Exploration does: `main_ship_space_command_receipts` idempotency (replay-safe on (ship, request_id); failure reasons write NO receipt), the S2 canonical lock order, `mainship_space_validate_context` (requires settled `in_space`) + `mainship_space_assert_cross_domain_exclusion`. It reads `mining_fields` DOWNWARD (hidden server-only data; a field is revealed only at the moment of extraction) and ACCRUES the pending bundle on its own state (`pending_bundle_json` = the field's deterministic bundle, `secured_at` NULL, extracting ship in `main_ship_id`). **FIELD DEPLETION (WORLD-BALANCE-P19, 0137):** entirely inside `if world_balance_enabled` (so mining is byte-identical while dark), the writer reads `worldstate_field_remaining(field)` DOWNWARD and scales each item qty by the reserve with a per-item floor of 1 (`greatest(1, round(qty × reserve))`) BEFORE snapshotting the bundle (diminishing returns), then calls `worldstate_deplete_field(field)` EXACTLY ONCE per real extraction — placed in the success path right after the extraction-row insert (unreachable on a replay, which returns at the receipt lookup before any insert), so no double-deplete. While dark reserve = 1.0 → the stored bundle is verbatim today's and deplete is never called. Mining never writes `mining_field_state` (World-State-owned — depletion goes through the `worldstate_deplete_field` writer-function). **Securing (0105):** Mining's OWN cron `process_mining_securing()` *(60s; internal/service_role; `FOR UPDATE SKIP LOCKED`)* deposits a pending bundle once the carrying ship (row's ship, else the player's canonical ship) is settled **SAFE** — `spatial_state in ('home','at_location')` per the 0055 state model, never in_transit/in_space/destroyed/legacy — by calling `Reward.grant('mining', extraction_id, player, base, bundle)` — **THE sole secured-deposit owner, exactly as the exploration processor and `process_fleet_movements` call it** — then sets `secured_at` (idempotency DOUBLE-guarded: `secured_at` + `reward_grants` UNIQUE (source_type, source_id)). IN-FLIGHT SAFETY (0064/0100 precedent): the processor does NOT check the flag — accrued value is never stranded by an emergency flag-off; inert while the command is dark (no rows can exist). **Read surface (0106):** `get_my_mining_extractions()` *(read-only, dark-gated `mining_disabled` — the 0087/0101 read idiom; the ONLY client path to field data, strictly post-extraction: it joins the caller's OWN extraction rows to the hidden fields, so an un-extracted field stays unreachable by construction; repeatable history — one row per extraction, bundle = the row's `pending_bundle_json` snapshot, never the field's own composition directly)*. Bundles are ITEMS-ONLY (`{items[]}` drawn from the existing `ore`/`crystal`/`artifact_core` catalog rows) — mining NEVER lands anything in `base_resources` (no second landing path to the Base-owned scalars). Edges (all DOWNWARD, acyclic, the Exploration shape): Mining → OSN geometry/locks (read) · Main Ship (read) · Reference/Config (cfg reads) · Bases (read: deposit target) · Reward (grant) · **World State (WORLD-BALANCE-P19, 0137 — read `worldstate_field_remaining` + call the writer-function `worldstate_deplete_field`, both DOWNWARD; World State never calls into Mining, so acyclic)**; nothing calls into Mining. Forfeiture of pending value: DEFERRED (0100 posture — pending rows wait) | write mining_fields (no runtime writer exists) · write `mining_field_state` directly (depletion ONLY via `worldstate_deplete_field`) · deposit outside `Reward.grant('mining', extraction_id, …)` (never write inventory/base tables directly) · write base_resources at all (items-only bundles) · write another activity's tables · expose an un-extracted field's coordinates/composition to a client · extract while the gate is off |

| **Modules** | module_types, module_recipe_ingredients *(catalog/config — seeded by migration only, no runtime writer)*; module_instances *(0108; owner-read, individual rows never counts, no fitting columns until Phase 14)* | `modules_mint_instance(player, module_type, key)` *(0108 — THE ONE writer of `module_instances`; internal leaf, service_role only, exception-style errors like Inventory's writers; idempotent by the NOT NULL UNIQUE `mint_key` — `on conflict do nothing`, a replayed key returns the EXISTING instance id and can never mint twice, the `inventory_deposit(p_key)` semantics; key namespacing is the producer's contract. EVERY producer — the Phase-13 craft command and any future `build_orders` queue completion (the M4.5 retirement path) — must mint THROUGH it; no second insert path, ever. Its ONE live caller is Production's craft command `production_craft_module`/`craft_module` (0109))*, plus `get_my_module_instances()` *(0110 — read-only, dark-gated `module_crafting_disabled`, the 0101/0106 read idiom: the caller's OWN instances joined to their `module_types` catalog identity, newest first; authenticated-only. Deliberately NO catalog/recipe RPC — `module_types`/`module_recipe_ingredients` are public-read Reference/Config catalogs read by direct client select like `item_types`, unlike the hidden sites/fields that forced 0101/0106; and NO inventory-balance join — the client reads its own `player_inventory` via the existing 0039 own-row grant, no new cross-system read edge)*. The catalog/recipe tables are public read-only Reference/Config-posture catalogs (0039/0042 idiom). **DARK**: `module_crafting_enabled=false` (0107) — every Phase-13 RPC added in later slices must check it FIRST and reject-before-any-read. **The craft COMMAND belongs to Production** (ROADMAP law 5: "Production=support craft/crafting · Fitting=modules") and SHIPPED as `craft_module` (0109 — see the Production row): an instant idempotent player RPC (PLAYER-scoped request_id receipts `module_craft_receipts`, the `trade_relief_claims` keying — crafting is non-spatial, so NOT the ship-scoped space receipts) fanning out one-directionally DOWNWARD to Inventory (`inventory_spend` for the items-only ingredient costs) and Modules (the ONE mint helper) — acyclic, no new cycle. One craft = one instance (no batching). DEFERRED with a retirement note: the M4.5 `build_orders` "same queue" integration — when module production later moves onto the serial queue, the queued completion path MUST call the SAME Modules mint helper (never a second mint path) | write player_inventory/inventory_ledger/base_resources (ingredients spend ONLY via `Inventory.inventory_spend`) · mint an instance outside the ONE Modules mint helper · touch fitting/`module_slots` (Phase 14) · client writes · craft while the gate is off |

| **Fitting** | ship_module_fittings *(0112; owner-read; the `module_instance_id` PK IS the one-ship-per-module invariant)*; module_fitting_receipts *(0113; owner-read; PK (player_id, request_id))* | `fit_module_to_ship(module_instance, ship, request_id)` / `unfit_module_from_ship(module_instance, request_id)` *(authenticated; the TWO thin client entries — 0109 wrapper idiom: auth → anti-probe flag gate FIRST → delegate with the fixed action → the ONE shared reason→code/message mapper `fitting_command_client_envelope` (extracted once because two wrappers would duplicate 0109's inline map); **DARK** behind `module_fitting_enabled=false` (0111))* → private `fitting_execute_command(player, action, module_instance, ship, request_id)` *(service_role/internal — ONE private command for BOTH actions precisely so `module_fitting_receipts` keeps ONE sole writer. 0109 order matched exactly: dark gate FIRST (reject-before-any-read) → request_id validation → per-player advisory lock BEFORE the replay check (the SAME `('module_fitting', player)` key as `fitting_apply` — `pg_advisory_xact_lock` is reentrant within a transaction, so the writer's nested acquisition is safe and the replay check is serialized with all fitting mutations) → verbatim replay from the receipt's stored `result_json` (trade semantics, no payload-conflict check) → action-shape validation ('fit' requires a ship, 'unfit' forbids one → `invalid_request`) → the GAME RULE this layer owns (0114 settled-SAFE correction of the 0113 `'home'` literal): the affected ship (target on fit; currently-fitted ship on unfit) must be SETTLED-SAFE — `mainship_space_validate_context` ok AND its validated state in `('home','at_location')` (the 0100/0105 securing-processor SAFE set verbatim), then `mainship_space_assert_cross_domain_exclusion` ok (the exact 0099/0104 activity-command companion machinery — no active legacy movement / pointer mismatch / presence conflict) — a composite that since 0121 lives in the ONE shared Main-Ship leaf `mainship_space_assert_settled_safe` (extracted behavior-identically when Captain became its second consumer; the command was re-created as a pure refactor) — else ONE truthful reject `ship_not_settled` (constrained state transitions — a loadout never changes mid-transit/in-space/mid-combat; stats are frozen for an expedition's duration; fail-closed: legacy NULL, in_space, in_transit, destroyed, and any incoherent context all reject; satisfiable today — commissioned ships sit `at_location`) → delegate the mutation to `fitting_apply` (NEVER touches `ship_module_fittings` directly — the sole-writer law; writer reasons pass through: `module_not_owned`/`ship_not_owned`/`already_fitted`/`not_fitted`/`insufficient_slots`) → only a SUCCESSFUL mutation writes a receipt (failures write nothing — the 0109 law))*, plus the internal `fitting_apply` (0112 — see the `ship_module_fittings` §1 row: THE sole writer of the fitting table; enforces the structural invariants: ownership, one-ship-per-module PK + `already_fitted` naming the current ship, and the Σ `slot_cost` ≤ `module_slots` HARD CAP (reject, never clamp — 0044:112–115); called today ONLY by `fitting_execute_command`), plus `get_my_ship_fittings()` *(0116 — read-only, the 0101/0106/0110 read idiom with the gate FIRST (before auth, per the slice spec): dark-rejects `{ok:false, reason:'module_fitting_disabled'}` identically for every caller while the flag is false — the frontend's server-driven visibility signal — then returns ONLY the caller's own `ship_module_fittings` rows (query-scoped `player_id = auth.uid()`, defense in depth over RLS) joined downward via `module_instances` to `module_types` display identity (name/slot_type/slot_cost), `fitted_at` desc + uuid tiebreak; authenticated-only. Deliberately NO catalog RPC — the 0110 stance: the catalogs are public-read direct client selects — and NO ship `module_slots` in this RPC: limits come from the client's own `main_ship_instances` rows or `get_my_expedition_preview` (0115 carries `module_slots_used/limit`))*. Edges (all DOWNWARD, acyclic): Fitting → Modules (read `module_instances`) · Main Ship (read `main_ship_instances` ownership + `module_slots`) · OSN context helpers (read-only — `mainship_space_validate_context` + `mainship_space_assert_cross_domain_exclusion`, 0114; the exact 0099/0104 reuse-never-reinvent posture) · Reference/Config (read `module_types.slot_cost` + the `module_fitting_enabled` flag). Inbound: the Expedition-stats adapter `calculate_expedition_stats` (0115) READS `ship_module_fittings` DOWNWARD (read-only — the ship's fit set feeds capacity-capped stats); still NOTHING writes through Fitting but its own command | write module_instances/main_ship_instances/module_types (reads only — never a second writer on another system's table) · mutate ship_module_fittings outside `fitting_apply` (no second insert/update/delete path, ever) · write module_fitting_receipts outside `fitting_execute_command` · clamp an over-capacity fit (hard reject only) · silently re-home an already-fitted module · expose the private command/writer/mapper to clients · fit/unfit while the gate is off |

| **Captain** | captain_types, captain_recipe_ingredients *(0125; catalog/config — seeded by migration only, no runtime writer)*; captain_instances *(0118; owner-read, individual rows never counts; + additive `xp`/`level` (0177) — sole progression writer = `captain_xp_accrue()`)*; ship_captain_assignments *(0119; owner-read; the `captain_instance_id` PK IS the one-ship-per-captain invariant)*; captain_assignment_receipts *(0120; owner-read; PK (player_id, request_id))*; captain_counted_grants *(0177; server-only; the per-(grant, captain) XP consumption ledger with the NULL-captain sentinel — see its §1 row)* | `captains_mint_instance(player, captain_type, key)` *(0118 — THE ONE writer of `captain_instances`; internal leaf, service_role only, exception-style errors like Inventory's/Modules' writers (0039/0108); idempotent by the NOT NULL UNIQUE `mint_key` — `on conflict do nothing`, a replayed key returns the EXISTING instance id and can never mint twice; key namespacing is the producer's contract. Its ONE caller is now Production's recruit command `production_recruit_captain` (0126 — mint key `recruit:<player>:<request_id>`); any future acquisition path must mint THROUGH this leaf; no second insert path, ever)*, plus `assign_captain_to_ship(request_id, captain_instance, ship)` / `unassign_captain_from_ship(request_id, captain_instance)` *(authenticated; the TWO thin client entries — the 0113 wrapper idiom: auth → anti-probe dark gate FIRST (while dark, EVERY caller gets the identical literal `{ok:false, reason:'captain_assignment_disabled'}` — the 0110/0116 reason-keyed read-surface signal, a locked adaptation of 0113's code-keyed envelopes) → delegate with the fixed action → the ONE shared reason→envelope mapper `captain_command_client_envelope` (the captain analogue of 0113's fitting-coupled mapper, extracted once because two wrappers would duplicate the map); **DARK** behind `captain_assignment_enabled=false` (0117))* → private `captain_execute_command(player, action, captain_instance, ship, request_id)` *(service_role/internal — ONE private command for BOTH actions precisely so `captain_assignment_receipts` keeps ONE sole writer. 0113 order matched exactly: dark gate FIRST (reject-before-any-read) → request_id validation → per-player advisory lock BEFORE the replay check (the SAME `('captain_assignment', player)` key as `captain_assign_apply` — reentrant within the transaction, so the nested acquisition is safe and the replay check is serialized with all assignment mutations) → verbatim replay from the receipt's stored `result_json` (trade semantics, no payload-conflict check) → action-shape validation ('assign' requires a ship, 'unassign' forbids one → `invalid_request`) → the GAME RULE this layer owns (0121 — the promised 0114-analogue amendment, delivered): the affected ship (target on assign, owner-scoped read answering `ship_not_owned`; the currently-assigned ship on unassign, read owner-scoped from `ship_captain_assignments` — an unassigned captain SKIPS the rule and gets the writer's truthful `not_assigned`) must be SETTLED-SAFE via the shared Main-Ship leaf `mainship_space_assert_settled_safe` (a captain roster, like a module loadout, never changes mid-transit/in-space/mid-combat; fail-closed), else ONE truthful reject `ship_not_settled` → delegate the mutation to `captain_assign_apply` (NEVER touches `ship_captain_assignments` directly — the sole-writer law; the writer's exception-style reason-prefixed raises are translated into failure envelopes in a guarded block, UNKNOWN errors re-raised — never hide a bug) → only a SUCCESSFUL mutation writes a receipt (failures write nothing — the 0109/0113 law))*, plus the internal `captain_assign_apply` (0119 — see the `ship_captain_assignments` §1 row: THE sole writer; enforces ownership both sides by the (id, player) pair, the one-ship-per-captain PK + truthful `already_assigned`, and the `count(*) < captain_slots` headcount hard cap — reject, never clamp; its ONE caller is `captain_execute_command`), plus `get_my_captain_instances()` / `get_my_ship_captains(ship)` *(0123 — the read surface, read-only, the 0110/0116 idioms respectively: dark gate on `captain_assignment_enabled` BEFORE any row read, rejecting the identical `{ok:false, reason:'captain_assignment_disabled'}` for every caller (the ONE visibility signal the 0120 wrappers share); own-rows ONLY (query-scoped `player_id = auth.uid()`, defense in depth over RLS) joined to the public `captain_types` catalog identity; the instances RPC adds a per-row assigned `main_ship_id` indicator via a LEFT JOIN to `ship_captain_assignments` — read-only display data, no new writer or dependency direction; the per-ship RPC validates the ship by the (main_ship_id, player_id) pair (`ship_not_owned`) and deliberately returns NO counts (the 0116 omission mirrored: limits come from the client's own `main_ship_instances` rows or `get_my_expedition_preview`, which carries `captain_slots_used/limit` since 0122); NO catalog RPC — `captain_types` is public-read direct client select, the 0110 stance; authenticated-only. C2-3 (0181): the instances RPC was re-created from its 0123 TRUE head under parity discipline with ONE marked hunk — the projection additionally carries the additive 0177 `xp`/`level` columns (xp=0/level=1 everywhere while `captain_growth_enabled` is dark, so the client XP bars render nothing today); the per-ship RPC is untouched — catalog identity only, no instance progression column belongs there)*. Everything stays DARK behind `captain_assignment_enabled=false`; the client surface exists but is server-rejected identically for every caller. The adapter feed (hard-capped by `captain_slots` as a headcount) arrives with the later adapter slice. Edges (all DOWNWARD, acyclic): Captain → Main Ship (read `main_ship_instances` ownership + `captain_slots`, plus the shared settled-SAFE leaf `mainship_space_assert_settled_safe`, 0121 — read-only) · Reference/Config (the `cfg_bool` flag read — new in 0120) · its own `captain_types`/`captain_instances` (intra-system); inbound: the two authenticated wrappers (the only client entries), and the Expedition-stats adapter `calculate_expedition_stats` (0122) READS `ship_captain_assignments` DOWNWARD (read-only — the ship's roster feeds headcount-capped stats, the 0115 Fitting precedent); still NOTHING writes through Captain but its own command. **Recruit recipe config (0125):** `captain_recipe_ingredients` — the normalized items-only recruit recipe (one implicit recipe per `captain_type`, `captain_memory_shard` the shared gating ingredient), a public-read catalog like `captain_types`, seeded by migration, NO runtime writer. Its consumer is now Production's recruit command `production_recruit_captain` (0126), which reads it DOWNWARD alongside the mint leaf — inbound edges INTO Captain: Production → Captain recipe read + Production → Captain `captains_mint_instance` mint (the leaf's first caller). The write still lands only through Captain's OWN sole writer (`captains_mint_instance`), so no second writer appears; acyclic, one sole-writer per table, and NO Captain→Inventory edge (Captain stays a pure instance-leaf — the recipe CONFIG is Captain's, the recruit COMMAND is Production's, which owns the Inventory spend). Whether the recruit surface is reachable is gated DARK by `captain_progression_enabled=false` (0124). **CAPXP progression (0177):** `captain_xp_accrue()` *(service_role/cron ONLY — never a client RPC; jobname `captain-xp-accrue`, every 5 min; gate-first on `captain_growth_enabled` → no-op envelope while dark, NEVER a raise)* — THE sole writer of `captain_counted_grants` AND of `captain_instances.xp/level` (the mint leaf never touches them): one commit-safe statement (the 0145 anti-join idiom, per-GRANT with per-(grant, captain) credit rows + the NULL-captain sentinel) folds each FINALIZED `reward_grants` row exactly once into the captains CURRENTLY assigned (`ship_captain_assignments`) to the grant's derivable ships (combat manifest ∪ solo fleet tag / discovery scanner / extraction ship), flat per-source knobs (10/6/4 seeds), `level = 1 + floor(sqrt(xp / 100))` maintained inline (read by NOTHING until C2-2). New edges all DOWNWARD, acyclic: Captain → Reward (`reward_grants` read — the Ranking 0130 precedent) · Combat (`combat_encounters` read) · Movement (`fleets` tag read) · Team Command (`group_sortie_members` manifest read) · Exploration/Mining (ship-link reads) | write captain_types or captain_recipe_ingredients (migration-seeded Reference/Config, no runtime writer) · mint an instance outside the ONE Captain mint helper · mutate ship_captain_assignments outside `captain_assign_apply` (no second insert/update/delete path, ever) · write captain_assignment_receipts outside `captain_execute_command` · move `captain_instances.xp/level` outside `captain_xp_accrue` (Combat never writes captain XP mid-tick — XP reads finalized grants exactly like Ranking does) · write captain_counted_grants outside the accrual · write main_ship_instances or any other system's table (reads only; no Inventory/Base/Wallet edge exists) · clamp an over-headcount assign (hard reject only) · silently re-home an already-assigned captain · expose the private command/writer/mapper/mint leaf to clients · assign/unassign while the gate is off · accrue XP while `captain_growth_enabled` is off |

| **Ranking** | ranking_seasons *(0127; public read-only; the Ranking-owned root table — a season is a named scoring WINDOW per cadence, `weekly`/`monthly` may run concurrently over the same finalized events (independent leaderboards); a partial unique index enforces AT MOST ONE `active` season per cadence; reset-by-season = a NEW season row, NEVER deletion of event data)*, ranking_standings *(0128; public read-only; the per-(season_id, player_id, dimension) leaderboard score row — `dimension` is EXACTLY the `reward_grants.source_type` domain (the 0096 closed set `('combat','trade','exploration','mining')`), 1:1 with NO translation; OVERALL is DERIVED at read time (sum across dimensions), never stored; `last_counted_at` is the incremental/idempotent high-water mark; leaderboard index `(season_id, dimension, score desc)`; reset by a new `season_id`, old rows never deleted)*, ranking_counted_grants *(0144 schema / 0145 writer; SERVER-only per-(season_id, grant_id) consumption ledger making accrual COMMIT-SAFE; sole writer = `ranking_accrue_standings` (0145); see the Role note)* | `ranking_season_open(cadence, starts_at, ends_at, label)` *(0129 — PRIVATE, `SECURITY DEFINER`, **service-role-only** (no player wrapper — season management is a server/cron/admin op), DARK-gated on `ranking_enabled` FIRST; THE sole writer of `ranking_seasons`; idempotent on the natural key (cadence, starts_at); per-cadence advisory lock; opening a new active window CLOSES the prior active one of the same cadence (NEVER deletes it — reset by season, not deletion), a raced index trip → clean `code:'conflict'`)*. `ranking_accrue_standings()` *(0130, made commit-safe in 0145 — PRIVATE, `SECURITY DEFINER`, **service-role-only**, DARK-gated FIRST; THE sole writer of `ranking_standings` AND `ranking_counted_grants`; a global advisory-locked batch fold over ALL active seasons, made COMMIT-SAFE (0145) by a per-(season, grant) ANTI-JOIN against the `ranking_counted_grants` ledger (0144) — NOT the old `last_counted_at` timestamp watermark — idempotent + exactly-once; per-event value = the single-source `ranking_score_delta(rewards)` helper (0130 — IMMUTABLE: metal + item quantities, from the confirmed 0040 bundle keys); dimension = `rg.source_type` (1:1); cron-driven every 5 minutes (`ranking-accrue-standings`, 0147) — a dark-gated no-op while `ranking_enabled='false'`, so it begins accruing only when the human flips the flag)*. Role: a **READ-ONLY downward leaf consumer of finalized events** — it reads `reward_grants` DOWNWARD (the idempotent finalized-event source: UNIQUE (source_type, source_id) — one row per SECURED activity result; `source_type` ('combat','trade','exploration','mining') the standings `dimension` (1:1), `player_id` the leaderboard subject, `granted_at` the season-window field) and folds each grant into the matching `ranking_standings` dimension row COMMIT-SAFELY (0145) via a visibility-based per-(season, grant) ANTI-JOIN against the `ranking_counted_grants` ledger (0144) — every finalized grant not yet marked for an active season it falls within is folded and marked in ONE statement; a late-committing grant (whose txn-start `granted_at` predates a prior run) is simply absent from the ledger and picked up next run, so it is NEVER skipped (the old `granted_at > last_counted_at` timestamp watermark was commit-UNSAFE and is retired as the correctness cursor — `last_counted_at` is now informational only). Exactly-once (`unique (season_id, grant_id)`), idempotent, bounded by each ACTIVE season's `[starts_at, ends_at]` (a closed season stops accruing but keeps its rows); it writes ONLY its own tables. Read surface (0131 — READ-ONLY, dark-gated, PUBLIC): `get_ranking_seasons()` *(the browsable season list — season_id/cadence/label/starts_at/ends_at/status)* + `get_ranking_leaderboard(season_id, dimension, limit=100)` *(one season's ranked board; `dimension` ∈ the 4 concrete literals OR `'overall'`; validates the season (`unknown_season`) + dimension (`invalid_dimension`), clamps `limit` to [1,500]; attaches a 1-based `row_number()` rank over `score desc, player_id`)*. **OVERALL is DERIVED AT READ TIME** (sum of a player's per-dimension scores across the season) — NEVER a stored row (the slice-1 Must-NOT). Both are `stable security definer`, SELECT only Ranking's OWN public tables, add NO writer/table, and are granted to **anon + authenticated** (public leaderboards — the 0127/0128 public-read posture; unlike the captain own-roster surface's authenticated-only), each dark-gated FIRST (identical `{ok:false, code:'feature_disabled'}` while dark — anti-probe). Edges (all DOWNWARD, acyclic): Ranking → **Reward (read `reward_grants` — the first REALIZED cross-system read, 0130)** · Reference/Config (`cfg_bool('ranking_enabled')` read — from `ranking_season_open`, `ranking_accrue_standings`, and both read RPCs); Reward never reads Ranking and nothing calls into Ranking, so the call graph stays ACYCLIC. **DARK** behind `ranking_enabled=false` (0127): every scoring/read/season RPC checks it FIRST and rejects-before-any-read | write any other system's table (READS `reward_grants` DOWNWARD, never writes it or any activity/inventory/base table) · be written by any non-Ranking function (sole writers = Ranking's own `ranking_season_open` + `ranking_accrue_standings`) · store a denormalized OVERALL score (overall is derived at read time only) · reset scores by deleting/truncating finalized event data OR closed-season standings (a reset is a NEW active season row only; closing a season never deletes it or its scores) · score/read/manage a season while the gate is off |

| **Location Investment** | location_investments *(0132; owner-read; the Phase-18 root table — an APPEND-ONLY, monotonic per-contribution ledger: `investment_id` PK, `player_id`→`auth.users`, `location_id`→`locations`, `amount` numeric CHECK (>0), `invested_at`, `request_id` with unique (player_id, request_id) — the 0109 `module_craft_receipts` player-scoped idempotency keying)* | `invest_in_location(p_ship, p_amount, p_request_id)` *(0133 — authenticated public wrapper; auth → DARK gate on `location_investment_enabled` FIRST (anti-probe `feature_disabled`) → resolve the caller's OWNED ship via `mainship_resolve_owned_ship` (`ship_not_owned`) → delegate; `revoke from public, anon`, `grant to authenticated`)* → private `location_investment_invest(p_player, p_ship, p_amount, p_request_id)` *(0133 — `SECURITY DEFINER`, service-role/internal, THE SOLE writer of `location_investments`. The 0109 order: dark gate again (defense in depth) → `request_id` null-check (uuid, market_buy-bounded) → per-player advisory lock `('location_investment', player)` BEFORE the replay check → verbatim replay if a `(player, request_id)` ledger row exists (the ROW IS THE RECEIPT — no separate receipts table, no payload-conflict check) → resolve the CURRENTLY DOCKED location via `mainship_resolve_docked_location` (`not_docked`) → validate `amount > 0` AND `>= cfg_num('location_investment_min_amount')` (`invalid_amount`, nothing spent) → the ONE-WAY SINK `wallet_debit(player, amount)` DOWNWARD (`insufficient_credits`, NO row written) → append exactly ONE ledger row (`location_id` = the resolved dock) → `{ok:true, investment_id, location_id, amount, invested_at}`. A raced `unique (player_id, request_id)` trip rolls back the debit at the sub-block savepoint and replays — never a double debit. NO withdrawal / NO payout path anywhere. The uuid `request_id` param bridges to the text ledger column via `::text`)*. Read surface (0134 — all `stable security definer`, dark-gated on `location_investment_enabled` FIRST, identical anti-probe `{ok:false, code:'feature_disabled'}` while dark; ALL read-only, NO writer/table): `get_location_development(location_id)` *(PUBLIC — the persistent-state-vs-seasonal-score view for ONE location: `all_time_total` + `contributor_count` (persistent development, all-time SUM/distinct-owner count) and `season_total` (SUM within the current window); `unknown_location` if absent; aggregates across owners but exposes ONLY totals, never another player's rows)* + `get_location_investment_leaderboard(location_id, limit=100)` *(PUBLIC — the seasonal board: players ranked by their windowed SUM to that location, `score desc, player_id`, 1-based `row_number()`, limit clamped [1,500]; `unknown_location`)* + `get_my_location_investments()` *(authenticated — the caller's OWN history, the 0106/0110 idiom: query-scoped `player_id = auth.uid()` joined to `locations` for the name, newest first)*, all over the ONE shared season-window helper `location_investment_current_window()` *(0134 — `stable`, INTERNAL/client-revoked; THE single definition of "the current season window": `k = floor((now − epoch)/period)`, `[window_start, window_end)` from `cfg_num('location_investment_season_epoch_seconds')` (2026-01-01Z anchor) + `cfg_num('location_investment_season_seconds')` (604800 = weekly); every windowed read calls it — the window math lives in exactly one place, so NO season table and NO duplicated arithmetic)*. Role: a **NEW DARK downward LEAF**. PERSISTENT STATE = the all-time SUM per location and SEASONAL SCORE = a player's windowed SUM are **TWO READS over the ONE append-only ledger**, both DERIVED at read time — never a stored denormalized row (the 0131 law). The window is derived DETERMINISTICALLY from config (period + epoch), so it does **not** duplicate Ranking's `ranking_season_open` machinery and adds **no** season coupling to Ranking; reset-by-season shifts the windowed read while the ledger is untouched. Edges — REALIZED, all DOWNWARD and acyclic: Location Investment → **Wallet** (`wallet_debit` — the one-way credit sink, 0133) · **Main Ship** (read-only via `mainship_resolve_owned_ship` + `mainship_resolve_docked_location`, 0133) · **Map** (read `locations` for validation/display identity, 0134) · **Reference/Config** (`cfg_bool('location_investment_enabled')` + `cfg_num` min-amount/season tunables). Wallet / Main Ship / Map / Config never call into Investment, so the call graph stays ACYCLIC. **DARK** behind `location_investment_enabled=false` (0132) | withdraw or pay out (investment is a strict ONE-WAY SINK — the no-infinite-exploit guard; `amount > 0`, no value ever returns) · write any other system's table (writes ONLY its own `location_investments`; credits go DOWN through `wallet_debit`, never `player_wallet` directly) · mutate `location_investments` outside `location_investment_invest` (no second insert/update/delete path, ever) · store a denormalized persistent/seasonal aggregate as a second write path (both are derived at read time) · delete/truncate the ledger to "reset" (reset is by season WINDOW only; the ledger — persistent state — is never touched) · duplicate Ranking's season machinery or couple to `ranking_seasons` · be written by any non-Investment function · invest/read while the gate is off |

| **World Events** *(PHASE20-POLISH, 0139/0140/0141)* | world_events *(0139; server-only; the Phase-20 root table — presentational timed world happenings: `event_type` notice/world_state/seasonal, polymorphic `scope` global/zone/location with a scope↔target CHECK, `zone_id`/`location_id` Map FK targets, `title`/`body`/`severity`/`is_active`/`starts_at`/`ends_at`, plus the nullable-unique `dedup_key` 0140)* | `world_events_publish(event_type, scope, zone_id, location_id, title, body, severity, starts_at, ends_at, dedup_key)` *(0140 — SECURITY DEFINER, **service-role only**, client-revoked; THE sole insert path. DARK GATE FIRST (no-op → NULL while `phase20_polish_enabled=false`); validates event_type/scope/severity + the scope↔target invariant mirroring the 0139 CHECKs + Map-FK existence (a DOWNWARD read of static `zones`/`locations`); idempotent — a non-null `dedup_key` returns the EXISTING event id on the partial-unique conflict (never a duplicate), a NULL key is an ad-hoc event)* + `world_events_set_active(event_id, is_active)` *(0140 — SECURITY DEFINER, **service-role only**; the retire/reactivate path — flips `is_active` + bumps `updated_at`, NEVER deletes (no destructive cleanup); DARK GATE FIRST (no-op while false)).* NO client publish/retire path — server-authoritative, structurally no player-to-player event injection. Read surface: `get_world_events(p_location_id, p_zone_id)` *(0141 — `stable security definer`, **authenticated only**; the ONLY client path to `world_events`. FAIL-CLOSED FIRST: while `phase20_polish_enabled=false` returns an EMPTY `{ok:true, events:[]}` WITHOUT reading the table (server-rejected-while-dark, read-side consumer of the master flag). Enabled: returns only events that are BOTH currently LIVE (`is_active` + `starts_at <= now()` + (`ends_at is null` or `> now()`)) AND IN SCOPE (`global` always; `zone`/`location` only when the caller passed the matching id), presentational columns only, ordered severity-rank then newest. The client PASSES its display context (`p_location_id`/`p_zone_id` — coordinates it already holds from the map), so the read adds NO cross-system call-edge; world events are public presentational info (no per-player secret), so no server-side ship-position resolve is needed. Reads ONLY `world_events` + the master flag; writes nothing)* | write `zone_state`/`location_state` (World State's tables — NOT a second writer) · write fleets/combat/`reward_grants` or any other system's table · grant rewards (a downward LEAF — reads only the static Map for FK validation, the one-directional pipeline law) · mutate `world_events` outside `world_events_publish`/`world_events_set_active` (no second write path) · DELETE an event to retire it (status-flip only) · expose a client publish/retire path (service-role only) · publish/read/retire while the gate is off |

| **UI Assets** *(PHASE20-POLISH, 0142)* | *(NONE — the `ui_asset_catalog` table is Reference/Config, SEED-MIGRATION only, NO runtime writer)* | `get_ui_asset_catalog(p_asset_kind)` *(0142 — `stable security definer`, **authenticated only**; the ONLY client path to `ui_asset_catalog`. FAIL-CLOSED FIRST: while `phase20_polish_enabled=false` returns EMPTY `{ok:true, assets:[]}` WITHOUT reading the table; enabled: returns active vocabulary rows, optionally filtered by `p_asset_kind`, ordered `(asset_kind, sort_order, asset_key)`. Reads ONLY `ui_asset_catalog` + the master flag; references nothing → a pure downward leaf, no new call-edge; writes nothing)* | add a RUNTIME writer to `ui_asset_catalog` (seed-migration only) · write any other system's table · grant rewards · expose a client write path · read while the gate is off |

> **OSN geometry leaf.** `osn_distance(ax,ay,bx,by)` (0099) is a pure **IMMUTABLE** function owning no table
> and calling nothing — a downward leaf like Wallet. Activities consume it DOWNWARD (Exploration now, Mining
> later). The shipped movement writers keep their identical inline formula (below the duplication bar); they
> should adopt the helper whenever they are next re-defined for real changes.

> **Settled-SAFE leaf.** `mainship_space_assert_settled_safe(ship)` (0121) is THE ONE definition of "this
> ship is settled and safe to act on": `mainship_space_validate_context` ok AND its validated state in
> `('home','at_location')` (the 0100/0105 securing-processor SAFE set) AND
> `mainship_space_assert_cross_domain_exclusion` ok — fail-closed (legacy NULL / in_space / in_transit /
> destroyed / any incoherent context all answer false). Main-Ship-owned (the `mainship_space_*` family,
> 0056), owns no table, reads only through the two 0056 helpers, service_role-only. It was 0114's inline
> block, extracted behavior-identically the moment Captain became a second consumer (the no-duplication hard
> rule; 0114's own header had recorded "no extraction needed" while there was ONE site). Consumers, both
> DOWNWARD: Fitting (`fitting_execute_command`, re-created in 0121 as a pure refactor) and Captain
> (`captain_execute_command`, 0121). Callers resolve WHICH ship per their own action semantics; the leaf
> answers only whether that ship is settled. The exploration/mining commands deliberately do NOT consume it —
> their accepted state is `in_space` (they are open-space actions), a different rule.

> **On-demand OSN arrival settle.** `command_main_ship_settle_arrival(ship?)` (0150 — UX-CLEANUP item 6A;
> authenticated, gated on the EXISTING `mainship_space_movement_enabled`) lets the owner settle their OWN
> due `main_ship_space_movements` row immediately instead of waiting for the 30s
> `process-mainship-space-arrivals` cron (which is UNCHANGED and remains the backstop). It adds NO writer
> and NO settlement logic: after the §2.5 ownership resolver (`mainship_resolve_owned_ship`) it mirrors the
> cron's claim sequence verbatim — SKIP-LOCKED `mainship_space_lock_context` (contention → `busy`, the cron
> wins; never blocks), coherent `in_transit` validate, cross-domain exclusion, movement re-read under lock
> (`status='moving' AND due`), full fleet-linkage check — then invokes the cron's OWN primitives
> (`mainship_space_dock_at_location` / `mainship_space_settle_space_arrival`), whose `status='moving'`
> guards make a cron/on-demand race exactly-once (loser no-ops as `already_settled`). Not-due / no-movement
> calls are safe no-ops. No reward/spend path exists to re-enter (OSN settlement writes only
> movement/fleet/ship/presence state). The client fires it once per movement id at `arrive_at`
> (`useSettleDueArrival` due-trigger); poll cadences and the cron are untouched.

> **Legacy in-flight spatial representation (0152).** `mainship_mark_legacy_in_flight(ship, status)` is THE
> ONE write expressing "legacy movement family → ship in the NULL (legacy) spatial representation":
> `status ∈ ('traveling','returning')` (raises on anything else) + `spatial_state=NULL` + `space_x/y=NULL`
> in one statement — a Main-Ship-owned leaf, service_role/internal only (its SECURITY DEFINER callers invoke
> it as owner). Its callers — `send_main_ship_expedition` / `move_main_ship_to_location` (departure) and
> `request_main_ship_return` (return) — were re-created in 0152 body-verbatim with ONLY that one ship write
> changed (CREATE OR REPLACE preserves their client grants). *(0152 also re-created
> `command_main_ship_stop_transit` to call this helper with `'returning'`, but 0155 replaced Stop's
> return-home settlement with a hold-in-open-space that writes `main_ship_instances` INLINE to
> `(stationary, in_space)` — so Stop is NO LONGER a caller of this helper.)*
> RECORD CORRECTED: `main_ship_instances.spatial_state` is NOT written only by the OSN-3/4 coordinate
> writers — the legacy movement family also writes it, always to NULL (the legacy domain; it never claims
> `in_transit`/`in_space`, whose coordinate-movement linkage `mainship_space_validate_context` enforces).
> This closed the LIVE `ss_at_location_status` violations: a canonically-docked (`at_location`) ship
> departing or returning via the legacy surface now drops cleanly into the legacy domain. The 0055 lifecycle
> CHECKs are unchanged. RETIREMENT: the helper retires together with its four callers when the legacy
> `fleet_movements` main-ship family is replaced by the OSN coordinate domain. The other half — settling the
> ship on legacy ARRIVAL — shipped in 0153 (next blockquote).

> **Canonical docked-ship write (0153).** `mainship_mark_docked_at_location(ship)` is THE ONE write
> expressing "main ship → canonically docked" (`status='stationary', spatial_state='at_location',
> space_x/y=NULL`) — a Main-Ship-owned leaf, service_role/internal only, with exactly TWO callers, both
> DOWNWARD: the OSN Dock-0 writer `mainship_space_dock_at_location` (0067 body re-created in 0153 with its
> inline dock-branch ship write swapped for the helper; its terminal-failure `in_space` write is unchanged)
> and legacy Movement's `movement_settle_arrival` (0151 body re-created in 0153: after `fleet_set_present` +
> `presence_create`, a main-ship fleet's location-arrival now settles the SHIP — to the canonical docked pair
> when `mainship_space_location_target_legal` passes, or to NOTHING when the target is an active `'none'` but
> NON-dockable location, leaving the ship in the constraint-legal `legacy_present` NULL representation from
> its 0152 departure write; the `main_ship_id IS NOT NULL` gate keeps ordinary unit fleets untouched). NO
> second writer to `main_ship_instances`: both docking routes go through this one helper, and the docked-pair
> write exists NOWHERE else. NEW CALL EDGE (read-only, downward): `movement_settle_arrival` →
> `mainship_space_location_target_legal` (the STABLE dockability predicate leaf, 0067) — both new edges point
> at leaves that call nothing, so the call graph stays acyclic. `process_mainship_space_arrivals` /
> `process_fleet_movements` / `command_main_ship_settle_arrival[_legacy]` are untouched — they delegate to
> the two re-created functions and inherit the fix. RETIREMENT: with the legacy `fleet_movements` main-ship
> family's replacement by the OSN coordinate domain, Dock-0 becomes the helper's only caller and the write
> may fold back inline (same condition as 0152's in-flight helper).

> **Trade fan-out is acyclic.** Trade Market → {Wallet, Trade Cargo}, Trade Market → Main Ship (read-only, via
> `mainship_resolve_docked_location`), and Main Ship → Wallet are all one-directional DOWNWARD edges. Wallet and
> Trade Cargo are leaves — each writes only its own table and calls nothing above it. (The Trade Market →
> Main-Ship-read edge already existed inline in the RPCs; it is now the single named helper.) Each of `trade_goods` / `ship_cargo_lots` / `player_wallet` / `trade_receipts` has
> exactly **one** sole-writer and **no second writer anywhere**. The whole trade feature stays DARK while
> `trade_market_enabled=false`. `trade_relief_claims` is Trade-Market-owned with exactly one
> sole-writer, `market_claim_relief` (DARK behind `trade_relief_enabled=false`), and no second writer anywhere. Its
> writer adds no new cross-system edge: the relief credit flows through `wallet_credit` (Wallet remains the sole
> `player_wallet` writer), and its `player_wallet`/`ship_cargo_lots`/`main_ship_instances` accesses are all
> DOWNWARD reads — so Wallet stays a downward leaf and the fan-out stays acyclic.

> **Trade Market → World State price-composition edge is acyclic (WORLD-BALANCE-P19, 0136).** All three trade
> functions compose the static `market_offers` base price with the World-State price multiplier via the ONE helper
> `trade_effective_price` → `worldstate_current_price_multiplier(loc)` — a NEW DOWNWARD read edge
> Trade Market → World State. ACYCLIC: World State's only reads are its OWN `location_state` + `combat_reports`
> (0135); it never reads Trade Market, so no cycle and no two-way dependency. World State stays the SOLE writer of
> `location_state` (the tick writes `price_multiplier`); Trade Market writes only `trade_receipts` and NEVER writes
> `market_offers` (still static Reference/Config, no runtime writer) or `location_state`. The whole composition is a
> no-op while `world_balance_enabled='false'` (multiplier 1.0), so charged/paid/displayed prices are byte-identical
> to pre-slice.

> **Mining → World State field-depletion edges are acyclic (WORLD-BALANCE-P19, 0137).** `mining_extract`
> reads `worldstate_field_remaining(field)` (to scale its bundle by the reserve) and calls the writer-function
> `worldstate_deplete_field(field)` (to draw the reserve down once per real extraction) — two NEW DOWNWARD edges
> Mining → World State (an activity depending on the world-state leaf). ACYCLIC: World State never reads or calls
> Mining, so no cycle. World State stays the SOLE writer of `mining_field_state` (the tick regen +
> `worldstate_deplete_field`); Mining writes only its own `mining_extractions` and NEVER writes `mining_fields`
> (still static, no runtime writer) or `mining_field_state`. Both the bundle scaling and the deplete call live
> inside `if world_balance_enabled`, and both World State functions self-gate — so mining is byte-identical while
> `world_balance_enabled='false'`. Mining still deposits ONLY via `Reward.grant('mining', …)`.

> **World State → World Events narration edges are acyclic (EV-1, 0182).** `worldstate_tick` now CALLS
> `world_events_publish` DOWNWARD to NARRATE threshold happenings (pressure high/eased, depletion warnings, drift
> extremes — ALL STATE-detected, dedup-keyed per UTC day), and READS the leaf's `world_events` rows DOWNWARD
> read-only for ONE purpose (the eased noise-suppression: only narrate easing when TODAY's `pressure_high` was
> announced). The call goes INTO the World-Events leaf's OWN sole writer — `world_events` gains NO second write
> path (the tick never inserts it directly; pinned by the 0182 self-assert), and World State gains NO new write
> target of its own. ACYCLIC: World Events reads only its own table + the static Map and never reads or calls
> World State. NARRATE-NEVER-MUTATE (the World-Events charter, restated): events are presentational —
> publication never writes `zone_state`/`location_state`/`mining_field_state`/fleets/rewards; the reacting systems
> remain the owners. DOUBLE-DARK: every publication sits inside the tick's `world_balance_enabled` gate AND
> `world_events_publish` self-gates on `phase20_polish_enabled` (no-op → NULL) — zero events unless BOTH flags are
> true. D2 CRON-SAFETY: EVERY publication is its OWN begin/exception subtransaction (per location-event, per
> depleting field; `query_canceled` re-raised, everything else → WARNING and the tick proceeds) — one failure can
> neither abort the heartbeat nor roll back a sibling's success, and because detection is STATE-based the failed
> condition still holds next minute: the retry is genuine and the per-day dedup keeps it exactly-once.

> **Ranking read-edge is acyclic.** Ranking is a READ-ONLY downward leaf consumer of finalized events. Its only
> cross-system edges are DOWNWARD reads: Ranking → **Reward** (`ranking_accrue_standings` reads `reward_grants`,
> 0130 — the first realized edge) and Ranking → **Reference/Config** (`cfg_bool('ranking_enabled')`, from
> `ranking_season_open` (0129), `ranking_accrue_standings` (0130), and the read RPCs `get_ranking_seasons` /
> `get_ranking_leaderboard` (0131)). Ranking WRITES only its own tables (`ranking_seasons` via
> `ranking_season_open`; `ranking_standings` via `ranking_accrue_standings` — one sole writer each); the read
> RPCs SELECT only Ranking's own public tables and write nothing (`'overall'` is derived at read time, never
> stored). Reward never reads Ranking, and nothing calls into Ranking, so the graph stays acyclic and no
> second writer touches any Ranking table. The whole feature stays DARK while `ranking_enabled=false` (0127);
> writers and read RPCs all reject-before-any-read, and accrual is cron-driven every 5 minutes (`ranking-accrue-standings`, 0147) — a dark no-op until the human flips `ranking_enabled`.

---

## 3. The only allowed call-edges (5 entry points)

Player RPCs and cron processors are **orchestrators**. They call system functions in
order and never write another system's tables.

**`send_fleet_to_location(base, location, units)`** *(player RPC)*
→ validate (ownership · units available & positive · location valid/active/unlocked ·
mission allowed · fleet limit · not-already-assigned)
→ `Base.reserve_units` → `Fleet.create` → `Movement.create` → `Fleet.set_moving`

**`process_fleet_movements()`** *(cron 30s · `FOR UPDATE SKIP LOCKED` · idempotent on `resolved_at`;
since 0151 its loop body IS the shared per-movement helper `movement_settle_arrival` — byte-equivalent)*
- outbound arrival → `Movement.mark_arrived` → `Fleet.set_present` → `Presence.create`
  *(→ `WorldState.register_presence` + `Activity.start` → `Combat.create_encounter` for hunt)*
- return arrival → `Movement.mark_arrived` → `Base.merge_units` → `Fleet.complete`

**`command_main_ship_settle_arrival_legacy(fleet?)`** *(player RPC, 0151 — UX-CLEANUP item 6B)*
→ gate `mainship_send_enabled` → resolve/validate (owned · main-ship fleet · sole in-flight when null)
→ claim the due movement FOR UPDATE SKIP LOCKED (contention → busy; cron wins) → refuse non-'none'
location targets (never drives combat init) → `movement_settle_arrival` — the SAME helper the cron runs
*(one settlement body; cron races resolve exactly-once by its `status='moving' AND due` guard)*

**`command_main_ship_stop_transit(fleet)`** *(player RPC, 0149 → 0155 — Stop = HALT-AND-HOLD)*
→ gate `mainship_send_enabled` → validate (owned · main-ship fleet) → claim the `status='moving'`
`fleet_movements` row FOR UPDATE (no moving row: ship already held → `already_held`, else `already_settled`;
due row → `arrived`, left for the cron) → compute the interpolated halt point → **HOLD (no return home):**
terminate the movement row `status='cancelled'` (honest terminal, cron skips it) → settle the fleet to the
movement-less terminal `status='completed', location_mode='movement', active_movement_id=NULL, current_*=NULL`
(guarded `status='moving'`, raise+rollback on miss) → hold the ship `status='stationary',
spatial_state='in_space', space_x/y=halt point` (the already-legal 0055/0054 shape; the OSN-stop
representation, 0064:319-322)
*(single settlement path — the terminated row is never re-touched by `process_fleet_movements`; no reward
interaction: main-ship targets are activity 'none', payload stays '{}'. Response: `{stopped:true, held:true,
space_x, space_y}` — no arrive_at. 0155 replaced the 0149/0152 return-home transform; Slice B adds legacy
re-departure from the held position, Slice C the client copy)*

**`process_combat_ticks()`** *(cron 10–15s · locked · idempotent on `last_resolved_at`/`ended_at`)*
→ load units via `Fleet.get` → spawn wave → `Fleet.apply_losses` → accrue pending
reward on encounter → insert `combat_round`
→ on end: rewards ride the return movement as a `{metal?, items[]}` bundle and are
secured by `Reward.grant` **on home arrival only** *(→ `Base.add_resources` for metal,
`Inventory.inventory_deposit` for items)* → `Report.create` →
`Presence.complete` *(→ `WorldState.unregister_presence`)* →
`Movement.create(return)` + `Fleet.set_returning`, **or** `Fleet.destroy` on death

**`request_leave_location(presence)`** *(player RPC)*
→ validate → `Presence.set_retreating` → `Combat.set_retreating`
*(return movement created later by the combat tick after `retreat_delay_seconds`)*

**`process_location_state_ticks()`** *(cron 60s)* → `WorldState.tick()` only
*(`worldstate_tick()` READS `combat_reports` DOWNWARD (history, read-only) for the WORLD-BALANCE-P19
pirate-pressure danger term — a NEW acyclic edge World State → Report; it still writes ONLY
`location_state`/`zone_state` and never fleets/combat/rewards. That read is skipped entirely while
`world_balance_enabled='false'` — the tick's output is byte-identical to pre-slice, 0135.
The **world-balance world-tick is this SAME 60s cron** — phase-19 (0135/0136/0137) folded
pirate-pressure/price-drift/field-depletion INTO `worldstate_tick()`, so it needs NO cron of its own;
a second schedule would double-tick every dynamic. Dark no-op while `world_balance_enabled='false'`.
**EV-1 (0182) rides this SAME cron too**: the tick's STATE-detected threshold-event narration (via
`world_events_publish`, dedup-keyed per UTC day, one failure-guarded subtransaction PER publication so
a publish error never aborts the tick and genuinely retries next minute) added NO schedule —
double-dark behind `world_balance_enabled` × `phase20_polish_enabled`.)*

**`ranking_accrue_standings()`** *(cron 5min · `ranking-accrue-standings`, 0147 · global advisory-locked ·
commit-safe + idempotent via the 0144/0145 `ranking_counted_grants` anti-join)* → folds finalized
`reward_grants` into `ranking_standings` for all active seasons; reads `Reward.reward_grants` DOWNWARD,
writes ONLY its own Ranking tables. **Dark-gated no-op while `ranking_enabled='false'`** — installed now
(the only genuinely-unscheduled background job), accrues only once the human flips the flag. 5-minute
cadence: standings are a slow incremental aggregate and the fold is idempotent, so cadence affects only
leaderboard freshness, not correctness (a late run folds the backlog next firing, none skipped).

### Explicitly forbidden edges
- Movement → never writes `base_units`/`fleet_units`/`combat_*`.
- Combat → never writes `base_resources` (only via `Reward.grant`); never writes
  `fleet_movements` except via `Movement.create`.
- Presence → never writes `location_state` (only via `WorldState.*`) or `combat_*`.
- World State → never writes fleets/combat/rewards/`combat_reports`. It MAY READ `combat_reports`
  DOWNWARD (history, read-only) for the WORLD-BALANCE-P19 pirate-pressure danger term (0135) — an
  acyclic edge World State → Report (Report writes only `combat_reports` and calls nothing back). This
  is a passive world dynamic over finalized history, not a read of active state (Report stays
  never-a-source-of-truth-for-active-state), and it is skipped while `world_balance_enabled='false'`.
  It MAY CALL `world_events_publish` DOWNWARD to narrate and MAY READ `world_events` read-only (the
  eased noise-suppression) (EV-1, 0182) but never writes `world_events` directly — World Events'
  sole-writer invariant holds; narration never mutates game state.
- Report → writes only `combat_reports`; never read as source of truth for active state.
- Client → writes nothing; calls RPCs only.

---

## 4. Beyond-spec ownership decisions (approved 2026-06-16)

1. **Fleet System** split from Movement — sole owner of `fleets`+`fleet_units`.
2. **Base System** — sole owner of `base_units`+`base_resources` via reserve/merge/add.
3. **World State System** — sole owner of `zone_state`+`location_state`; Map stays static.
4. **Reward ledger** `reward_grants` + idempotent `reward_grant()` = only reward path.
5. **Activity** owns no table in MVP — thin router / extension point.
6. **Support craft (Phase 6)** — `support_craft_types` is **Reference/Config metadata only**
   (public read, no client write, no functions). Capacity-limited loadout definitions
   (`capacity_cost`, role, activity tags, tradeoffs). **No instances, no expedition
   attachment, no capacity enforcement, no stat consumption yet** — combat still uses
   `unit_types` (separate namespace). A future main ship exposes finite `support_capacity`
   and `calculate_expedition_stats` consumes these (Phases 7–8).
7. **Main Ship (Phase 7)** — `main_ship_hull_types` = Reference/Config (public read);
   `main_ship_instances` = the Main Ship system (owner-read). It was originally one row per player via a
   `player_id` UNIQUE, but that UNIQUE was **dropped in migration 0079 (TRADE-FLEET-0C)** — a player MAY now
   own multiple `main_ship_instances` rows. Multi-ship is structurally allowed but stays **DARK**: the
   sole-ship behavior is now a runtime shim / dark gate (`mainship_additional_commission_enabled=false`), NOT a
   schema UNIQUE. Writes only through the Main-Ship server functions (`ensure/get/rename` + the priced
   commission path), all server-only. The hull
   exposes the finite `support_capacity` a future `calculate_expedition_stats` will enforce.
   **Phase 7 does NOT drive expeditions** — the ship sits `home`, untouched by combat/fleet/
   movement/production; nothing consumes it yet.
8. **Stat adapter (Phase 8; extended Phase 14 0115, Phase 15 0122)** — `calculate_expedition_stats(player,
   main_ship_id, loadout, activity)` owns **no table**; it is a **pure read/compute** function
   (reads `main_ship_instances` + `main_ship_hull_types` + `support_craft_types`, since
   0115 the ship's fitted modules via `ship_module_fittings` + `module_instances` +
   `module_types` — a DOWNWARD read-only edge into Fitting/Modules — and since 0122 the ship's
   assigned captains via `ship_captain_assignments` + `captain_instances` + `captain_types`, a
   DOWNWARD read-only edge into Captain; enforces `support_capacity`, `module_slots`, AND
   `captain_slots` as hard caps — over-capacity raises, never clamps; the captain cap is
   count-based, one captain = one slot — with slot_type/specialization
   tradeoffs mirroring the role rules; module and captain contributions flow through the SAME accumulators
   as support craft, plus their `speed_mult_bonus` sums applied additively inside the ONE
   multiplier before penalties; output gains exactly
   `module_slots_used`/`module_slots_limit` (0115) and `captain_slots_used`/`captain_slots_limit`
   (0122) — a zero-module, zero-captain ship returns the pre-0115 values for
   every pre-existing key). It **must NOT** mutate any
   state — no expeditions/fleets/combat/rewards/inventory/ranking/reports/ship/support counts.
   It is THE single source of expedition stats (ship + support craft + modules + captains — the
   "(later +captains)" note is redeemed by 0122), but is
   **not wired into live combat yet** — the proven fleet-stack path still owns outcomes.

These keep every architecture law enforceable with a single-writer-per-table rule.

---

## 5. Invariant checklist (every PR/migration must keep true)

- [ ] Each table still has exactly one writing system.
- [ ] Cross-system changes go only through the exposed functions above.
- [ ] Call graph remains acyclic (Combat→Movement only via `create(return)`).
- [ ] Activity remains table-less for MVP.
- [ ] `reward_grants` remains the only reward-application path.
- [ ] `combat_reports` remains history only, never source of truth.
- [ ] No client write path to any game-state table.
